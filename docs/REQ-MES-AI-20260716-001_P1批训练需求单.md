# P1 次核心资产理解批训练 · 需求单

| 项 | 内容 |
|----|----|
| 需求单编号 | **REQ-MES-AI-20260716-001** |
| 标题 | P1 次核心资产（表/字段 + 存储过程）理解批训练 |
| 提出 | AE（AI 工程）| 日期 | 2026-07-16 |
| 状态 | **已评审通过（TL 代批 2026-07-16）· 表批+过程批均已完成入库并验收前置就绪**（过程批临时预算闸 TL 授权 8M/仅本次，实耗 5.34M，翌日已恢复默认 300 万）|
| 关联 | Phase-1 里程碑（已评审通过）；排期 `docs/S3-4-3_Token复盘与P1P2排期.md`；P0 批训练 `REQ-MES-AI-20260715-001` |
| 类型 | 外部 AI 批训练（调 Kimi，经 ai-gateway 脱敏门）|

> **本单是执行 P1 训练的前置闸门**。依 CLAUDE.md §5.1（只接受评审通过的标准需求单）与 §12（预算管控），
> 在本单**评审通过**且 **TL 预算闸授权留痕**前，AI 不得发起 P1 批训练外部调用。

---

## A. 背景与依据

- Phase-1「真实 MES 系统理解」里程碑已评审通过：P0（Top100 表 / Top60 包）三大理解 + 断言/RAG/脱敏全交付。
- S3-4-3 排期确定 **P1（次核心）主动训练、P2（长尾）RAG 兜底 + 按需增量**。P1 现具备启动条件（清单已物化、单位成本已实测、管线已验证）。
- 目的：把 AI 的可验证理解从 P0 扩展到 P1 次核心资产，提升覆盖，为 Sprint6 断言回归与后续需求供料。

## B. 任务内容

沿用 P0 已验证管线，对 P1 资产做理解训练并入库：

1. **表/字段**：299 张 P1 表 → 表卡片（结构 + 字典列语义 + 依赖热度画像），复用 `build_table_cards.py` + `run_s3_training.py --phase tables`。
2. **存储过程**：140 个 P1 包体 → 切分子程序 → 逐单元理解卡片，复用 `plsql_splitter.py` + `run_s3_training.py --phase procs`（厂区/产线变体去重）。
3. **入库与验证**：卡片入 `mes_s3_understanding`（`ingest_s3_cards.py`）→ `validate_ingestion.py` 抽检召回；对 P1 P0 级资产补断言种子（`build_assertion_seeds.py`）。

## C. 范围与清单（已物化，零 P0 重叠）

| 层 | 选取口径 | 数量 | 清单（远程持久）|
|----|----|----|----|
| P1 表 | 依赖图中心度 rank 101–400，剔除 P0 与视图/SCOAPUSER | **299** | `/home/xintong/mes-s3-data/p1/p1_tables.txt` |
| P1 包体 | 依赖图中心度 rank 61–200（MESAPUSER），剔除 P0 | **140** | `/home/xintong/mes-s3-data/p1/p1_units.txt` |

- 分层与 P0 同口径（中心度 Top-N，非百分位 TIER 列）；score 区间：表 20→7、包 45→26。
- 已校验 P1 与 P0 表/包**均 0 重叠**，不重复训练。

## D. 预算与 §12 授权申请（关键闸）

| 项 | 估算 | 依据 |
|----|----|----|
| P1 表卡片 | ~299 × 5,602 ≈ **1.67M** | P0 实测 ~5.6K/表 |
| P1 子程序 | ~1,560 子程序 × 2,515 ≈ **3.9M** | P0 实测 ~2.5K/个；140 包保守 ~12 子程序/包、变体去重 7% |
| **P1 合计** | **≈ 5.6M tokens** | S3-4-3 |

- **超默认日预算**：5.6M 远超 §12 默认 300 万/日。**申请 TL 预授权临时预算闸**（同 P0 做法，留痕本单）。
- **建议分两窗口**：① 表批（~1.67M，默认闸内可跑）；② 过程批（~3.9M，须临时闸，建议独立训练日）。
- 沿用 P0 省 token 措施：输出上限 1200/单元、变体去重、3 分片并发、训练不走 RAG。
- 实际消耗以脱敏门网关计量为准；逼近授权闸即暂停并上报。

## E. 执行方案（评审通过 + 预算授权后）

```
① 选取已固化 → p1_tables.txt / p1_units.txt（本单 C）
② 拉 P1 包体源码：db_introspect.py --source-units-file p1_units.txt（测试库/只读自省）
③ 切分：plsql_splitter.py
④ 表批：build_table_cards.py（P1 表）→ run_s3_training.py --phase tables（经网关脱敏门）
⑤ 过程批：run_s3_training.py --phase procs --shards 3（经网关脱敏门；须临时预算闸）
⑥ 入库：ingest_s3_cards.py → mes_s3_understanding（同多语言模型，§14 合规）
⑦ 验证：validate_ingestion.py 抽检 Top-3 召回；补断言种子
```

## F. 验收标准

- 卡片产出率 ≥ 95%（表/子程序）；语义标注遵循「缺失标待确认、零臆断」。
- 卡片入 `mes_s3_understanding` 且核心资产 Top-3 可召回。
- Token 在授权闸内；超支即停并上报（§12）。
- 全程经脱敏门（§4.2 七类）；产出素材审计 0 命中方可入库。

## G. 前置检查（§5.2，执行前逐项确认）

```
□ 本需求单状态＝已评审通过
□ TL 已授权 P1 临时预算闸并在本单留痕（§12）
□ 测试环境可用、与生产网络隔离；生产库仅只读自省
□ 本任务不含生产环境写操作
□ P1 清单已固化且与 P0 零重叠（已校验）
□ 脱敏门在链路生效（网关唯一外发咽喉）
```

## H. 授权签字栏

| 角色 | 事项 | 结论 | 签字 | 日期 |
|----|----|----|----|----|
| IT 评审 | 需求评审通过 | ☑ 通过（TL 代批）| TL | 2026-07-16 |
| TL 技术负责人 | **表批预算授权**（上限 **200 万 tokens** / 有效期：本次表批）| ☑ 授权 | TL | 2026-07-16 |
| TL 技术负责人 | **过程批临时预算闸授权**（估 ~3.9M / 闸上限 **8M tokens** / 有效期：**仅本次过程批**，翌日恢复默认 300 万）| ☑ 授权 | TL | 2026-07-16 |
| TL 技术负责人 | **超大过程二次切分片段（L2）补训预算授权**（估 **~0.55–0.8M tokens** / 64 片段）：①次日预算重置后运行＝无需额外授权，走默认 300 万日闸；②若当日运行须临时上调闸 `L2_GATE≥6.5M`（覆盖今日累计 5.34M+）| ☐ **待签**（择一） |  |  |
| ITM IT 审核专员 | 脱敏合规确认（网关脱敏门链路强制生效）| ☐ 待签 |  |  |

> **执行范围（更新 2026-07-16）**：
> - 表批：已授权（上限 200 万）并**完成**——261 卡片（清 1 张 demo 垃圾表）→ 1002 chunk 入 `mes_s3_understanding`（5155→6157），实耗 ~65.2 万 tokens，核心表 Top-3 可召回。
> - 过程批：**TL 已授权临时预算闸（上限 8M / 仅本次）并完成**——DB 只读自省（`10.30.10.111:25521/XEPDB1`，凭据仅内网机 repo 外 env）→ 切分 139 包/2352 子程序 → 训练 2209 单元（3 分片, 输出上限 1200, 变体 0）产 **2206 卡片**，**实耗 5.34M tokens（< 8M 闸，网关计量一致）**；脱敏审计（源码 meta_p1 + 产出卡片 10.1万行）**0 命中**（§4.2 七类，`desensitize_audit_p1_procs.md`）；入库 8009 chunk 进 `mes_s3_understanding`（无 --reset，集合 6157→**14166**），核心过程 Top-3 可召回。**网关已恢复默认闸 300 万**。16 个 >1200 行超大单元留二次切分。
> - **超大过程二次切分（L2，2026-07-16，0 token 静态）已完成**：新增 `scripts/kb-ingest/plsql_second_split.py`（递归结构切分 + 覆盖率/尾段双断言 + 21 项 self-test + 14 项 pytest，kb-ingest 全套 268 通过，commit `96247d4`）。对 16 个超大过程（含 5811 行 `PR_MECH_ITEM_JDG` ×2）切出 **64 个 ≤1001 行片段、0 硬切**，**16/16 全过程逐字符重建通过**；产物 `p1/split_p1_l2.json` + `split_p1_l2_report.md`，补训编排 `orch_p1_procs_l2.sh` 已就绪（**未运行**）。修复切分中暴露的两个静默缺陷（终结 END 截断致 TITLE 丢 836 行；CASE 表达式列表误吞开块致深度失衡）。**L2 片段实训+入库为独立预算闸门步骤，待上表授权栏择一签字后执行。**

## 风险与回退

| 风险 | 缓解 |
|----|----|
| Token 超授权闸 | 分表批/过程批两窗口；逼近即暂停上报；输出上限+变体去重控量 |
| P1 语义偏差（低中心度、注释更稀）| 语义拼图多源；缺失标待确认；抽检 + BIZ 复核关键项 |
| 韩文/动态 SQL 盲区 | 沿用 P0 标注纪律，不臆断 |
| 入库向量空间不一致 | 复用同一多语言 embedding（§14）；换模型则全量重入 |

---

*本需求单依 CLAUDE.md §5.1/§12 生成；未签齐授权栏前，AI 仅完成清单物化等非外发准备，不发起 P1 批训练外部调用。*
