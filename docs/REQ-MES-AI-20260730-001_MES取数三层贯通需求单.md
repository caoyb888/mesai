# MES 数据取数（NL→Oracle 只读 SELECT）三层贯通 · 需求单

| 项 | 内容 |
|----|----|
| 需求单编号 | **REQ-MES-AI-20260730-001** |
| 标题 | MES 数据取数能力：自然语言生成 Oracle 只读 SELECT + 安全校验 + 只读执行（三层贯通） |
| 提出 | AE（AI 工程） | 日期 | 2026-07-30 |
| 状态 | **代码已落地并推送**（commit `07d50f6`，分支 `feature/real-mes-integration`，提交信息引用 REQ-MES-AI-20260716-001）；**真实执行授权待签**（当前降级运行） |
| 关联 | 任务规划 `docs/Task_MES_SQL_三层贯通规划_2026-07-30.md`；既有问答端点 `/v1/ai/mes-qa`（REQ-MES-AI-20260716-001 交付，本单**不动**）；知识库 `mes_s3_understanding`（14,410 chunk） |
| 类型 | 功能开发（在线推理调用，经 ai-gateway 脱敏门+预算闸） |

> **本单是新功能开发的前置闸门**。依 CLAUDE.md §5.1，评审通过且 H 栏三项治理授权签齐前，
> AI 不落地任何代码（不含 pom.xml 依赖新增与数据源接入）。

---

## A. 背景与依据

- mes-qa 已三层贯通（Vue → Spring Boot → FastAPI 网关），但仅支持**知识问答**（返回解释性文本），不支持**取数**（返回结果行）。
- 原始设计目标「NL 生成 Oracle SELECT + 只读执行」尚未恢复落地，本单补齐该能力，与 mes-qa **并列新增**。
- 两条成熟链路可直接复用，风险可控：
  - RAG 检索：`rag.retrieve_for_mes`（mes-qa 已验证，本地向量 0 外部 token）；
  - SQL 执行/数据源门控：`DemoService.executeSelect` / `ItsmDataSourceConfig` 的 `@ConditionalOnExpression` 模式（demo 模块已验证）。

## B. 任务内容

### B1. AI 网关（Python）— 新 `routers/mes_sql.py`，`POST /v1/ai/mes-sql`

- RAG：复用 `retrieve_for_mes(kind=auto)` 取表+过程上下文。
- 系统提示 `_MES_SQL_SYSTEM`：Oracle 方言、owner MESAPUSER、仅 SELECT、**禁止 SELECT \***、显式列名、参数化、只引用上下文中真实存在的表/字段（防臆造）、行数用 `FETCH FIRST`；输出 JSON `{sql, explanation, referenced_tables, referenced_columns, confidence, unanswerable_reason}`。
- 走 `gateway_chat` 管道（脱敏→预算→provider→计量）。
- `models.py` 加 `MesSqlRequest`/`MesSqlResponse`；pytest 覆盖检索调用/JSON 解析/不可答路径。

### B2. Spring Boot（Java）— 新模块 `module/messql`

- `MesSqlController` `POST /mes-sql/query`。
- `SqlSafetyValidator`（新，安全红线核心）：单语句、必须 SELECT 开头、拦截 DDL/DML/多语句/注释注入/SELECT *。
- `MesReadOnlyDataSourceConfig`：`@ConditionalOnExpression("${spring.mes.datasource.url:}")`、Hikari `read-only=true`、Oracle 驱动、`mesJdbcTemplate` bean。
- 执行：`mesJdbcTemplate.queryForList(oracleRowCap(sql, 200))`，Oracle 行数封顶 `SELECT * FROM (...) WHERE ROWNUM <= 200`；未配置数据源 → `executed=false` + 原因（优雅降级）。
- DTO/VO：`MesSqlRequest(question/topN/executeSql)`、`MesSqlVO`（复用 `SqlExecutionResult`/`ContextDocVO`）。
- `application-test.yml` 的 `spring.mes.datasource` 为 Oracle 占位（env 注入，零硬编码）。
- JUnit：validator 全用例 + service（mock 网关 + null 数据源降级）+ 行数封顶单测，覆盖率 ≥70%。

### B3. 前端（Vue）— 新页 `views/mes-sql/MesSqlPage.vue`

- 输入 + 示例 + 执行开关；结果区：生成 SQL（复制按钮）+ 解释 + 引用表标签 + el-table 结果 + 截断提示 + RAG 上下文折叠 + token/延时标签 + 只读安全徽标。
- `api/mesSql.js`、router、`MainLayout.vue` 菜单项。

### B4. 资产化（治理配套）

- Prompt 落 `/prompts/sql/mes-sql-generation.md`（§6.2）。
- `/assertions` 新增 ≥3 条断言（§7.3）：SELECT 开头 / 禁 SELECT * / 只引用 KB 内表。

## C. 范围边界

| 在内 | 不在内 |
|----|----|
| NL→Oracle SELECT 生成 + 安全校验 + 只读执行（≤200 行封顶） | 任何写操作（DDL/DML 一律由 validator 拦截） |
| 降级路径（未配数据源时返回 SQL 不执行） | mes-qa 现有问答端点的任何改动 |
| 前端只读查询页面 | 远程真实 MES 只读账号的申请/开通（属甲方供数，另行办理） |

## D. 预算与 §12 说明

- 本功能为**在线单请求推理**（非批训练）：单次调用 ≈ RAG 上下文（本地 0 token）+ 1 次 LLM 生成（估 1–3K token/次）。
- 走默认 **300 万/日** 预算闸即可，**无需临时预算闸授权**；开发自测调用计入日预算。

## E. 执行方案（评审通过 + 授权签齐后）

```
① 网关层：routers/mes_sql.py + models + pytest → 网关全量测试
② Java 层：module/messql + SqlSafetyValidator + 门控数据源 + JUnit → mvn test 全绿
③ 前端层：MesSqlPage.vue + api + 菜单 → vite build 通过
④ 资产化：Prompt 模板 + ≥3 条断言
⑤ 降级路径端到端验证：未配 MES_DB_URL 时「生成+校验+返回 SQL 不执行」全链路跑通
⑥ 真实取数验证：待远程只读账号就绪后另行执行（不在本单本地验证范围）
```

## F. 验收标准

- 网关 pytest 无回归（现 61 通过基线上新增用例全过）；Java `mvn test` 全绿且新模块覆盖率 ≥70%；前端 `vite build` 通过。
- `SqlSafetyValidator` 对 DDL/DML/多语句/注释注入/SELECT * 的拦截用例 100% 通过。
- 降级路径：未配置数据源时端到端返回 `executed=false` + 生成 SQL + 校验结果，无异常。
- 生成 SQL 仅引用 RAG 上下文内真实表/字段（防臆造），断言库新增 ≥3 条且 Critical 全过。
- 全程经脱敏门（§4.2），零硬编码（§4.1），提交格式符合 §5.4。

## G. 前置检查（§5.2，执行前逐项确认）

```
□ 本需求单状态＝已评审通过
□ H 栏三项治理授权已签齐（依赖 / 数据源 / 验证边界）
□ 已从知识库确认复用链路现状（retrieve_for_mes / executeSelect / 门控模式）
□ 横向影响评估：不动 mes-qa、不动 demo 模块、不改既有配置默认值
□ 本任务不含生产环境写操作（执行目标为只读账号，且本地/测试先行）
```

## H. 授权签字栏

| 角色 | 事项 | 结论 | 签字 | 日期 |
|----|----|----|----|----|
| IT 评审 | 需求评审 | ☐ 待评审 |  |  |
| TL 技术负责人 | **治理门①**：`pom.xml` 新增 Oracle 驱动依赖（§2 破例授权，实际落地为 **ojdbc11**，随 `07d50f6` 入库） | ☑ 已授权（本次评审） | TL | 2026-07-18 |
| TL 技术负责人 | **治理门②**：只读 MES Oracle 数据源首次接入（§4.3/§一）。**2026-07-30 已授权创建只读账号**：`MES_READONLY` 建于 `10.30.10.111:25521/XEPDB1`（SYSTEM 执行，CREATE SESSION + 逐表 SELECT ×1860/1860 成功，写操作实测 ORA-01031 被拒，凭据存 repo 外 `mesdb.env`）；**`MES_DB_URL` 服务侧接线待确认后执行** | ☑ 账号创建已授权 / ☐ 接线待签 | TL | 2026-07-30 |
| TL 技术负责人 | **治理门③**：确认验证边界——本地仅验证生成+校验+降级+行数封顶单测；真实取数待远程只读账号就绪后另行验证 | ☐ 待签 |  |  |
| ITM IT 审核专员 | 脱敏合规确认（网关脱敏门链路强制生效） | ☐ 待签 |  |  |

## 风险与回退

| 风险 | 缓解 |
|----|----|
| LLM 臆造表/字段 | 系统提示强接地 + 只引用 RAG 上下文 + 断言库校验 + 不可答路径（unanswerable_reason） |
| 生成 SQL 含写操作 | SqlSafetyValidator 双层拦截（网关约束 + Java 强制校验），拦截用例全覆盖 |
| 数据源误接入生产 | 门控默认关闭；凭据仅 env 注入；目标账号必须为只读账号；未配置时物理上不执行 |
| 大结果集拖垮服务 | 行数封顶 ROWNUM ≤ 200 + 前端截断提示 |

---

*本需求单依 CLAUDE.md §5.1/§5.2 生成；签字栏未签齐前，AI 不发起任何代码落地。*
