# S3-3 业务流程理解报告（状态机反推 · T3-3-1）

| 项 | 内容 |
|----|----|
| 文件编号 | AI-MES-REPORT-S3-3-2026 |
| 需求单 | REQ-MES-AI-20260715-001（T3-3-1）|
| 日期 | 2026-07-15 |
| 反推状态字段 | 34（源自过程 set_assignments，代码字典解码）|
| Token | 23,962 |
| 验收目标 | 业务流程/状态机描述准确率 ≥90%（BIZ 复核）|

> 状态机由**过程静态写入证据**反推 + 字典解码 + AI 合成；**须 BIZ 对照真实业务复核**（尤其动态 SQL 与缺条件上下文处的「待确认」项）。本报告为 S3-3 第一部分（状态机），追溯链路(T3-3-2)/核心流程(T3-3-3)/验证题(T3-3-4)续。

## 状态字段一览

| 表.列 | 取值 | 写入过程数 |
|----|----|----|
| SCH_PLAN_HEAT.TUNDISH_CHG_FL | N, Y | 1 |
| SCT_IMPORT_TMP.PROCESS_FLAG | E, P | 1 |
| SQM_MTC_REQ.MTC_STS_CD | 2, 9 | 7 |
| SPR_FCE_CHARGE.MSG_FLAG | 1, 2 | 2 |
| SPR_FCE_DISCHARGE.MSG_FLAG | 1, 2 | 1 |
| SPR_ROLL_RSLT_NOW.PROCESS_FLAG | 1, 2 | 1 |
| SPR_DS_RSLT.PROCESS_FLAG | 1, 2 | 1 |
| SMS_HEAT.HEAT_CONF_YN | N, Y | 1 |
| SCR_INDICATOR_SOURCE.AUTO_FLAG | Y | 1 |
| SCH_PLAN_SLAB.PLAN_SLAB_STS | A7 | 1 |
| SCH_PLAN_ROLL.PLAN_ROLL_STS | A7 | 1 |
| SCH_INST_ROLL.PLAN_ROLL_STS | A7 | 1 |
| SCH_PLT_PLAN_ROLL.PLAN_ROLL_STS | A7 | 1 |
| SCH_PLT_INST_ROLL.PLAN_ROLL_STS | A7 | 1 |
| SCH_PLT_INST_CUTTING.PLAN_FSH_STS | A7 | 1 |
| SCH_CAST_DESIGN_RESULT.WK_INST_YN | 3 | 1 |
| SCH_PLT_WORK_DETAIL_INFO.WK_INST_YN | 1 | 1 |
| SCH_HSM_WORK_DETAIL_INFO.WK_INST_YN | 1 | 1 |
| SMS_SLAB.PROG_CD | PH1C | 1 |
| SYD_DISP_ORD_DETAIL_BATCH.UPT_FLAG | 1 | 1 |
| SYD_DISP_ORD_DETAIL_BATCH.READ_FLAG | 0 | 1 |
| SMM_TRANSMISSION_ERP_SUM.CANCEL_FLAG | 9 | 1 |
| SQM_SMP_LOTFORM_RSLT.REP_YN | N | 6 |
| SCH_SLAB_DESIGN_RESULT.DGN_MANUAL_FL | S | 1 |
| SCH_SLAB_DESIGN_ORD.SLAB_DGN_NORMAL_YN | E | 1 |
| SPR_HEAT_WAIT.SLAB_STS_CD | 3 | 2 |
| SPR_CB_LOAD.PROCESS_FLAG | 1 | 1 |
| SPR_MARK_INFO.PROCESS_FLAG | 1 | 1 |
| SPR_XM_DEFECT_LIST.LAST_FL | 1 | 1 |
| SPR_PLATE.HTM_INST_REQ_YN | Y | 1 |
| SPR_SPRB0330.USE_YN | N | 1 |
| SMS_HEAT.HEAT_STS | 2 | 1 |
| SMS_HEAT.LST_CUT_YN | N | 1 |
| SMS_RSLT_CUT.LST_SLAB_YN | N | 1 |

---

## 状态机反推明细

### 1. `SCH_PLAN_HEAT.TUNDISH_CHG_FL`

```markdown
## 状态字段的状态机理解

### 1. **状态清单**
- **N**：未接收/未完成/无（待确认）
  - 根据候选字典解码，N 可以对应“未接收”、“未完成”、“无”。由于没有具体的上下文信息，不能确定具体是哪一种情况，因此需要进一步确认。
- **Y**：已接收/完成/有
  - 根据候选字典解码，Y 对应“已接收”、“完成”、“有”。这表明当状态为 Y 时，相关的操作或流程已经完成或被接收。

### 2. **流转与触发**
- **流转顺序**：N → Y
  - 根据观测到的状态写入，状态从 N 流转到 Y 是由 `BSCH_PLAN_ADJUST` 过程触发的。这表明在计划调整过程中，某个条件或操作导致状态从“未接收/未完成/无”变为“已接收/完成/有”。

### 3. **业务含义**
- SCH_PLAN_HEAT.TUNDISH_CHG_FL 字段在业务流程中的作用是标识钢包更换（Tundish Change）的状态。这个字段可能用于跟踪和管理钢包的更换过程，确保在炼钢过程中钢包的更换是按照计划和要求进行的。具体来说，它可能涉及到钢包的准备、更换、以及更换后的确认等环节。

### 4. **不确定点**
- **状态 N 的具体含义**：由于缺乏具体的业务流程和上下文信息，状态 N 的具体含义（未接收/未完成/无）需要进一步确认。
- **状态 Y 的具体含义**：同样，状态 Y 的具体含义（已接收/完成/有）也需要根据实际的业务流程来确定。
- **状态流转的具体条件**：状态从 N 流转到 Y 的具体条件和业务逻辑需要进一步分析和确认，可能涉及到计划调整的具体规则和要求。
```

### 2. `SCT_IMPORT_TMP.PROCESS_FLAG`

```markdown
### 状态字段的状态机理解

#### 1. **状态清单**
- **E**: END
  - 业务含义：表示流程的结束或完成状态。根据候选字典解码，这可能与[INST_STS] END相对应，同时关联到[ORD_COAT_WGT_CD] 镀锌量120和[ORD_USAGE_GRP1] 再轧制产业(Rerolling)，以及[OS_MDF_TY] Order Completion。这表明流程已经完成，并且与特定的镀锌量和产业用途相关。
- **P**: PROCESS
  - 业务含义：表示流程正在进行中。根据候选字典解码，这可能与[INST_STS] PROCESS相对应，同时关联到[ORD_COAT_WGT_CD] 镀锌量450和[ORD_USAGE_GRP1] 石油化工产业（Petroleum），以及[OS_MDF_TY] Process Modification。这表明流程正在修改或调整中，与特定的镀锌量和产业用途相关。

#### 2. **流转与触发**
- 可能的状态流转顺序为：从`P`（PROCESS）流转到`E`（END）。
- 各转移由以下过程触发：
  - `P`到`E`：由`BSCT_COST_PUBLIC`过程触发，该过程可能负责将流程状态从进行中更新为结束。

#### 3. **业务含义**
- 该字段`SCT_IMPORT_TMP.PROCESS_FLAG`在业务流程中的作用是标识和记录流程的当前状态，特别是在成本计算和订单处理的上下文中。它帮助跟踪订单或工单的生命周期，从开始处理到最终完成，确保流程的每个阶段都能被正确记录和监控。

#### 4. **不确定点**
- 由于缺乏更多的上下文信息，以下点需要进一步确认：
  - **待确认**：`BSCT_COST_PUBLIC`过程的具体业务逻辑和触发条件。
  - **待确认**：`SCT_IMPORT_TMP.PROCESS_FLAG`字段在其他业务流程中是否有其他状态或用途。
  - **待确认**：是否存在其他过程也可能影响`SCT_IMPORT_TMP.PROCESS_FLAG`字段的状态变化。
```

### 3. `SQM_MTC_REQ.MTC_STS_CD`

```markdown
## 状态字段的状态机理解

### 1. 状态清单

- **2**：根据候选字典解码，此状态可能代表“订单外一级”、“订单外1级”、“维修”或“订单外1级”，考虑到多个解码指向相似的含义，可以合理推测该状态表示“订单外一级”或“维修”。由于存在多个相似但略有差异的解码，具体含义可能需要进一步确认。
  - **业务含义**：订单外一级/维修（待确认）

- **9**：根据候选字典解码，此状态明确表示“不合格”或“不可用”。考虑到多个解码指向相同的含义，可以确定该状态表示“不合格”。
  - **业务含义**：不合格

### 2. 流转与触发

- **流转顺序**：
  - 从状态2流转到状态9：由`BSQM_MTC_ISSUE_LZ`、`BSQM_MTC_ISSUE_RZ`、`BSQM_MTC_ISSUE_SHIP`等过程触发。
  - 由于没有提供从状态9回到状态2的过程，我们可以推测状态9可能是一个终止状态或者是一个长期状态，直到有进一步的业务动作发生。

### 3. 业务含义

该字段`SQM_MTC_REQ.MTC_STS_CD`在业务流程中的作用可能与材料检验和质量控制相关。具体来说，它可能用于标识和跟踪材料（如板坯）在生产过程中的质量状态，特别是在检验和发货环节。状态2可能表示材料在检验后被判定为符合订单要求或需要维修，而状态9则明确表示材料不符合质量标准，被判定为不合格。

### 4. 不确定点

- 状态2的具体业务含义需要进一步确认，因为它对应了多个相似但略有差异的解码，这可能涉及到不同的业务场景或条件。
- 状态9虽然解码较为一致，但由于缺乏具体的业务流程上下文，其在具体业务流程中的作用和触发条件仍需进一步确认。
- 由于缺乏从状态9回到状态2的过程信息，我们无法确定是否存在这样的流转，或者这种流转是否由特定的业务动作触发。
```

### 4. `SPR_FCE_CHARGE.MSG_FLAG`

### 状态机理解

#### 1. **状态清单**

- **1**：Steel Making（炼钢）状态，表示该批次的钢材已经通过冷矫直处理，并且质量等级为合格。（根据字典解码）
- **2**：Casting（铸造）状态，表示该批次的钢材是订单外一级，经过压力矫直处理，质量等级为订单外1级。（根据字典解码）

#### 2. **流转与触发**

- **1 → 2**：由`BSPR_OPER_L2`过程触发，可能表示从炼钢合格状态流转到铸造订单外一级状态。
- **2 ← 1**：同样由`BSPR_OPER_L2`过程触发，可能表示从炼钢合格状态流转到铸造订单外一级状态。
- **1 ← 1**：由`BSPR_HEAT_FCE`和`BSPR_OPER_L2`两个过程触发，可能表示在炼钢合格状态下的某种状态更新或确认。

#### 3. **业务含义**

`SPR_FCE_CHARGE.MSG_FLAG`字段在业务流程中的作用是标识钢材的制造状态和质量等级。这个字段记录了钢材在制造过程中的关键信息，如制造环节（炼钢或铸造）、质量等级（合格或订单外一级）以及矫直方式（冷矫直或压力矫直）。这些信息对于后续的钢材加工、质量控制和订单管理至关重要。

#### 4. **不确定点**

- **状态1和状态2的具体含义**：由于没有提供完整的业务流程和上下文，状态1和状态2的具体业务含义可能需要进一步确认。特别是“订单外一级”和“合格”的具体含义，以及它们在业务流程中的具体应用场景。
- **状态流转的具体条件**：状态流转的具体条件和触发逻辑没有明确的证据，需要根据实际业务流程和系统逻辑进一步确认。
- **状态写入的具体过程**：虽然可以推断出`BSPR_OPER_L2`过程可能与状态流转有关，但具体的写入逻辑和条件仍然需要进一步确认。

以上分析基于现有的状态写入证据和候选字典解码，具体的业务含义和状态流转逻辑可能需要结合实际的业务流程和系统设计进行进一步的确认和细化。

### 5. `SPR_FCE_DISCHARGE.MSG_FLAG`

```markdown
## 状态字段：SPR_FCE_DISCHARGE.MSG_FLAG

### 1. 状态清单
- **1**：Steel Making（炼钢）; 合格（APPRD_GRD）; 冷矫直（APPR_FIN_MTH_CD）; 合格（APPR_JDG_GRD）。
- **2**：Casting（铸造）; 订单外一级（APPRD_GRD）; 压力矫（APPR_FIN_MTH_CD）; 订单外1级（APPR_JDG_GRD）。

### 2. 流转与触发
- **流转**：根据观测到的状态写入，状态1和状态2均由`BSPR_OPER_L2`过程触发。
- **触发过程**：`BSPR_OPER_L2`可能是一个控制或操作过程，负责在不同条件下更新`MSG_FLAG`状态。

### 3. 业务含义
`SPR_FCE_DISCHARGE.MSG_FLAG`字段在业务流程中的作用可能与钢厂的生产和质量控制相关。具体来说，它可能用于标识和记录生产过程中的特定事件或状态，如炼钢和铸造的不同阶段，以及产品的质量等级。这个字段可能用于跟踪生产过程中的关键步骤，确保产品质量符合标准，并为后续的加工和销售提供必要的信息。

### 4. 不确定点
- **状态1和状态2的含义**：由于没有提供具体的业务流程文档，状态1和状态2的业务含义是基于候选字典解码的推测，需要进一步的业务确认。
- **状态流转的具体条件**：状态1和状态2之间的流转条件和顺序不明确，需要业务流程的具体描述来确认。
- **`BSPR_OPER_L2`过程的具体作用**：该过程如何根据业务逻辑触发状态更新，需要更多的上下文信息来确定。
- **状态写入的完整性**：仅观测到两个状态的写入，可能存在其他状态未被观测到，需要完整的状态写入证据来构建完整的状态机。
- **待确认**：状态1和状态2的具体业务含义和流转条件需要业务专家的确认。
```

### 6. `SPR_ROLL_RSLT_NOW.PROCESS_FLAG`

```markdown
### 状态字段的状态机理解

#### 1. 状态清单
- **1**: 根据候选字典解码，此状态可能表示“合格”的钢材，属于“Steel Making”（炼钢）环节，并且采用“冷矫直”作为最终矫直方法，判断结果为“合格”。由于只有一个过程`BSPR_OPER_L2`写入此状态，没有其他信息可以进一步确认其具体含义，因此标记为**[待确认]**。
- **2**: 根据候选字典解码，此状态可能表示“订单外一级”的钢材，属于“Casting”（铸造）环节，采用“压力矫”作为最终矫直方法，判断结果为“订单外1级”。同样，由于只有一个过程`BSPR_OPER_L2`写入此状态，没有其他信息可以进一步确认其具体含义，因此标记为**[待确认]**。

#### 2. 流转与触发
- 根据观测到的状态写入，`BSPR_OPER_L2`过程可以触发状态1和状态2。由于没有更多的过程信息和状态流转的证据，无法确定完整的状态流转顺序。因此，状态流转的具体路径**[待确认]**。

#### 3. 业务含义
- `PROCESS_FLAG`字段在业务流程中的作用可能是标识当前钢材的加工状态和质量等级。它可能用于跟踪钢材从炼钢到矫直的整个生产过程，并确保每个环节的质量控制符合标准。具体来说，它可能用于：
  - 区分不同生产环节（如炼钢和铸造）的钢材。
  - 标记钢材的质量等级（如合格和订单外一级）。
  - 指示钢材的最终矫直方法（如冷矫直和压力矫）。

#### 4. 不确定点
- 由于缺乏更多的过程信息和状态流转的上下文，无法准确确定状态1和状态2的具体业务含义，因此需要进一步的业务知识或数据来确认。
- 状态流转的具体路径和触发条件**[待确认]**，需要更多的过程调用和状态变化的证据。
- 状态字段`PROCESS_FLAG`的具体业务作用和上下文**[待确认]**，需要结合实际的业务流程和操作手册来进一步分析。
```

### 7. `SPR_DS_RSLT.PROCESS_FLAG`

```markdown
## 状态字段：SPR_DS_RSLT.PROCESS_FLAG

### 1. 状态清单
- **1**：[ABNR_REASON_GRP] 钢制造；[APPRD_GRD] 合格；[APPR_FIN_MTH_CD] 冷矫直；[APPR_JDG_GRD] 合格
- **2**：[ABNR_REASON_GRP] 铸造；[APPRD_GRD] 订单外一级；[APPR_FIN_MTH_CD] 压力矫；[APPR_JDG_GRD] 订单外1级

### 2. 流转与触发
- 根据观测到的状态写入，`BSPR_OPER_L2` 过程可以触发状态从 1 到 2 的变化，以及从 2 到 1 的变化。因此，可能的状态流转顺序为：
  - 1 → 2
  - 2 → 1
- 各转移由 `BSPR_OPER_L2` 过程触发。

### 3. 业务含义
`SPR_DS_RSLT.PROCESS_FLAG` 字段在业务流程中的作用可能与钢材的生产过程和质量控制有关。具体来说，它可能表示钢材在生产过程中的不同阶段或处理方式，以及对应的质量判断结果。例如：
- 状态 1 可能表示钢材在钢制造阶段，经过冷矫直处理后，质量被判定为合格。
- 状态 2 可能表示钢材在铸造阶段，经过压力矫直处理后，质量被判定为订单外一级。

### 4. 不确定点
- 由于缺乏更多上下文信息，状态 1 和状态 2 之间的具体业务逻辑和流转条件尚不明确，需要进一步确认。例如：
  - 状态 1 和状态 2 之间的转换条件是什么？
  - 这两个状态是否涵盖了所有可能的生产和质量控制情况？
- 因此，以上关于状态业务含义的解释均为「待确认」。
```

### 8. `SMS_HEAT.HEAT_CONF_YN`

```markdown
### 1. 状态清单

- **N (未接收/未完成/无)**
  - 业务含义：表示该加热炉配置尚未被接收或相关流程未完成，或者没有信用限制。
  - 不确定标注：由于候选字典解码中“N”对应多个含义，具体业务含义需要根据上下文进一步确认。

- **Y (已接收/完成/有)**
  - 业务含义：表示该加热炉配置已被接收，相关流程已完成，或者存在信用限制。

### 2. 流转与触发

- **N → Y**
  - 可能的流转：从“未接收/未完成/无”状态转变为“已接收/完成/有”状态。
  - 触发过程：由`BSMS_OPER_CC`过程触发，该过程可能负责更新加热炉配置的状态，从未完成到完成。

### 3. 业务含义

- 该字段`SMS_HEAT.HEAT_CONF_YN`在业务流程中的作用是标识加热炉配置的状态。它可能与加热炉的作业配置、审批流程或信用限制有关。具体来说，它可能用于控制加热炉的操作，确保在开始加热之前，所有必要的配置和审批都已经完成，并且信用限制得到满足。

### 4. 不确定点

- **状态“N”的具体业务含义**：由于“N”对应多个可能的业务含义，需要进一步的业务上下文来确定其具体含义，这需要「待确认」。
- **状态“Y”的具体业务含义**：同样，需要更多的上下文信息来确定“Y”状态的具体业务含义，这也需要「待确认」。
- **状态流转的具体条件**：由于缺乏具体的业务逻辑和条件，状态从“N”到“Y”的具体流转条件和业务规则也需要「待确认」。
```

### 9. `SCR_INDICATOR_SOURCE.AUTO_FLAG`

```markdown
### 状态字段：SCR_INDICATOR_SOURCE.AUTO_FLAG

#### 1. 状态清单
- **Y**：根据候选字典解码，可能的业务含义包括：
  - 已接收（ACCEPT_YN）
  - 完成（APPR_JDG_YN）
  - 采取上部和中间部分和下部（BEND_SMP_LTH_LOC）
  - 采取上部和中间部分和下部（BH_SMP_LTH_LOC）
  由于候选解码中有多个可能的含义，具体业务含义需进一步确认，因此标注为**待确认**。

#### 2. 流转与触发
- **流转顺序**：根据提供的证据，我们只能确定状态Y是由`BSCR_INDICATOR_ALL`过程写入的。由于没有提供其他状态的写入证据，无法推断完整的状态流转顺序。
- **触发过程**：状态Y的写入由`BSCR_INDICATOR_ALL`过程触发。

#### 3. 业务含义
- `SCR_INDICATOR_SOURCE.AUTO_FLAG`字段在业务流程中的作用**待确认**。由于缺乏具体的业务流程上下文，我们无法准确判断该字段在工单、质保书或板坯生命周期中的具体环节。可能与自动接收、自动完成或自动采样相关，但具体业务含义需要结合实际业务流程进一步分析。

#### 4. 不确定点
- **状态含义**：由于候选字典解码提供了多个可能的含义，而没有具体的业务流程上下文，无法确定`AUTO_FLAG`字段的具体业务含义，**待确认**。
- **状态流转**：由于只提供了一个状态的写入证据，无法推断完整的状态流转顺序，**待确认**。
- **业务流程环节**：由于缺乏具体的业务流程信息，无法确定该字段在业务流程中的具体作用，**待确认**。
```

### 10. `SCH_PLAN_SLAB.PLAN_SLAB_STS`

```markdown
# SCH_PLAN_SLAB.PLAN_SLAB_STS 状态机理解

## 1. 状态清单

- **A7**：计划返送（根据候选字典解码，此状态与“计划返送”相关联）

## 2. 流转与触发

- **A7 ← BSCH_PLAN_ADJUST**：状态 A7 由过程 `BSCHEME_PLAN_ADJUST` 触发写入。

## 3. 业务含义

`SCH_PLAN_SLAB.PLAN_SLAB_STS` 字段记录了板坯在生产计划中的当前状态。具体来说，它标识了板坯是否处于计划返送的状态。在生产过程中，这个字段帮助跟踪板坯的流转情况，特别是在需要将板坯返回到计划阶段进行重新处理或调整时。

## 4. 不确定点

- **状态 A7 的具体业务含义**：虽然根据候选字典解码，A7 与“计划返送”相关，但具体的业务流程和条件触发该状态的具体细节尚需进一步确认。因此，A7 的业务含义标为「待确认」。
```

### 11. `SCH_PLAN_ROLL.PLAN_ROLL_STS`

```markdown
### 1. 状态清单

| 值 | 业务含义 |
| --- | --- |
| A7 | 计划返送（根据候选字典解码确定） |

### 2. 流转与触发

- **A7 ← 由 BSCH_PLAN_ADJUST 过程写入**：根据观测到的状态写入证据，`A7`状态是由`BSC_PLAN_ADJUST`过程触发写入的。

### 3. 业务含义

`SCH_PLAN_ROLL.PLAN_ROLL_STS`字段记录的是计划卷的状态。具体来说，`A7`状态表示该计划卷处于“计划返送”的状态，可能是指在生产计划中，由于某些原因（如质量问题、生产调整等），该卷被标记为需要返送处理。

### 4. 不确定点

- **状态值的完整性**：目前只观测到`A7`这一个状态值被写入，不确定是否还有其他状态值存在。需要进一步的证据来确认完整的状态清单。**待确认**
- **状态流转的完整性**：仅根据一个状态写入证据，无法推断完整的状态流转顺序。需要更多的状态写入证据来确定完整的状态流转路径。**待确认**
- **状态触发的具体条件**：目前只知道`A7`状态是由`BSC_PLAN_ADJUST`过程触发的，但具体的触发条件和上下文信息尚不清楚。**待确认**
```

### 12. `SCH_INST_ROLL.PLAN_ROLL_STS`

```markdown
### 1. 状态清单
- **A7**：计划返送

### 2. 流转与触发
- **A7 ←** 由 `BSCHEME_PLAN_ADJUST` 过程触发

### 3. 业务含义
`SCH_INST_ROLL.PLAN_ROLL_STS` 字段记录了钢卷在生产计划中的当前状态。在业务流程中，该字段用于标识钢卷是否处于计划返送的状态。计划返送通常指的是由于某些原因（如质量问题、客户需求变更等），钢卷需要从当前的生产或存储环节返回到之前的环节进行处理或重新安排生产。

### 4. 不确定点
- 由于只观测到一个状态写入过程 `BSCHEME_PLAN_ADJUST`，无法确定该状态是否还有其他触发过程。需要进一步分析其他相关过程或业务逻辑来确认完整的状态流转路径。
- 状态 `A7` 的具体业务含义已根据候选字典解码确定为“计划返送”，但具体在业务流程中的作用和上下文需要结合实际业务场景进一步确认。
```

### 13. `SCH_PLT_PLAN_ROLL.PLAN_ROLL_STS`

```markdown
# SCH_PLT_PLAN_ROLL.PLAN_ROLL_STS 状态机理解

## 1. 状态清单
- **A7**：计划返送

## 2. 流转与触发
- **A7 ← BSCH_PLAN_ADJUST**：由`BSCN_PLAN_ADJUST`过程触发状态从其他状态转移到A7。

## 3. 业务含义
`SCH_PLT_PLAN_ROLL.PLAN_ROLL_STS`字段表示计划卷的状态，其中A7状态“计划返送”可能指的是在生产计划中，某些卷因为特定原因（如质量问题、订单变更等）需要返回到上游工序或者仓库，进行重新处理或者取消生产。

## 4. 不确定点
- 由于只提供了一个状态的写入证据，其他状态和流转关系**待确认**。
- `BSCN_PLAN_ADJUST`过程的具体业务逻辑和触发条件**待确认**。
```

### 14. `SCH_PLT_INST_ROLL.PLAN_ROLL_STS`

```markdown
## 状态字段状态机理解

### 1. **状态清单**
- **A7**：计划返送（根据候选字典解码）

### 2. **流转与触发**
- **A7 ← BSCH_PLAN_ADJUST**：由`BSCA_PLAN_ADJUST`过程将状态写为A7。

### 3. **业务含义**
- `SCH_PLT_INST_ROLL.PLAN_ROLL_STS`字段记录了钢厂生产计划中板材的计划返送状态。在生产计划调整过程中，若板材需要返送，则该字段会被更新为A7状态，表示计划中的板材将被返回处理。

### 4. **不确定点**
- 由于只观测到一个状态写入过程，无法确定其他可能的状态及其流转。其他状态和流转关系需要进一步的证据来确认。
- 状态A7的具体业务流程中的作用和触发条件需要更多的业务上下文来明确，当前信息不足以完全确定其在整个生产计划中的具体位置和影响。**待确认**
```

### 15. `SCH_PLT_INST_CUTTING.PLAN_FSH_STS`

```markdown
## SCH_PLT_INST_CUTTING.PLAN_FSH_STS 状态机理解

### 1. 状态清单
- **A7**：计划返送（根据候选字典解码，此状态对应于计划返送的业务含义）

### 2. 流转与触发
- **流转**：由于只观测到一个状态写入，即 `A7 ← BSCH_PLAN_ADJUST`，可以推断状态机至少包含从其他状态到 `A7` 的流转。
- **触发**：状态 `A7` 由 `BSCH_PLAN_ADJUST` 过程触发。

### 3. 业务含义
- `SCH_PLT_INST_CUTTING.PLAN_FSH_STS` 字段记录了计划完成状态，特别是在生产计划调整过程中，用于标识某个计划是否需要返送处理。在业务流程中，这可能涉及到对生产计划的调整和优化，以确保生产效率和资源的合理分配。

### 4. 不确定点
- 由于只观测到一个状态写入，其他可能的状态及其流转关系无法确定，需要进一步的证据来确认完整的状态机。
- 状态 `A7` 的具体业务流程环节和触发条件（如在什么情况下会触发 `BSCH_PLAN_ADJUST` 过程）需要更多的上下文信息来确认。
- **待确认**：状态 `A7` 是否还有其他触发过程，以及状态机中是否存在其他状态和它们之间的流转关系。
```

### 16. `SCH_CAST_DESIGN_RESULT.WK_INST_YN`

```markdown
### 1. **状态清单**

| 值 | 业务含义（含不确定标注） |
| --- | --- |
| 3 | Hot Rolling（待确认） |

### 2. **流转与触发**

- **流转顺序**：由于只观测到一个状态写入，无法确定完整的流转顺序。
- **触发过程**：状态值3由`BSCCH_PLAN_RSLT_CONF`过程触发写入。

### 3. **业务含义**

`SCH_CAST_DESIGN_RESULT.WK_INST_YN`字段在业务流程中可能用于标识某个工单或生产指令在热轧过程中的状态。具体来说，它可能表示工单是否已经完成热轧工序，或者是否需要进行热轧。由于只观测到一个状态值，无法确定该字段在业务流程中更详细的业务含义。

### 4. **不确定点**

- **状态值3的业务含义**：由于只观测到状态值3与`[ABNR_REASON_GRP] Hot Rolling`相关联，但缺乏更多上下文信息，无法确定该状态值是否仅表示热轧工序，还是包含其他含义。因此，状态值3的业务含义标为「待确认」。
- **完整的状态流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序，这需要进一步的证据来确认。
- **其他状态值**：由于没有观测到其他状态值的写入，无法确定该字段是否还有其他状态值及其业务含义，这些也需要进一步的证据来确认。
```

### 17. `SCH_PLT_WORK_DETAIL_INFO.WK_INST_YN`

```markdown
## 状态字段：SCH_PLT_WORK_DETAIL_INFO.WK_INST_YN

### 1. **状态清单**
- **1**：根据候选字典解码，此值可能代表“合格”。由于候选解码中包含“[APPRD_GRD] 合格”和“[APPR_JDG_GRD] 合格”，可以推断1表示某种形式的合格状态。但是，由于没有更多的上下文信息，这里的“合格”可能与不同的业务流程相关联，因此具体含义需要进一步确认。**待确认**

### 2. **流转与触发**
- **流转顺序**：由于只观测到一个状态写入，即从`BSCG_PLAN_RSLT_CONF`过程写入状态1，因此无法确定完整的状态流转顺序。可能存在从其他状态到状态1的流转，或者状态1到其他状态的流转，但这些流转过程没有在当前证据中体现。
- **触发过程**：状态1是由`BSCG_PLAN_RSLT_CONF`过程触发写入的。

### 3. **业务含义**
- `SCH_PLT_WORK_DETAIL_INFO.WK_INST_YN`字段在业务流程中可能用于标识某个工作指令或工序是否已经完成或符合质量标准。具体来说，状态1可能表示该工序已经完成并且符合质量要求，即“合格”。这个字段可能用于跟踪生产过程中的各个步骤，确保每个步骤都按照既定的标准执行，并且在质量控制环节中标记为合格。

### 4. **不确定点**
- **状态含义**：由于缺乏更多的上下文信息，状态1的具体业务含义不明确，需要进一步的业务流程文档或数据库字典来确认。**待确认**
- **状态流转**：当前证据不足以确定完整的状态流转路径，需要更多的过程调用和状态写入证据来构建完整的状态机。**待确认**
```

### 18. `SCH_HSM_WORK_DETAIL_INFO.WK_INST_YN`

```markdown
### SCH_HSM_WORK_DETAIL_INFO.WK_INST_YN 状态机理解

#### 1. 状态清单
- **1**：根据候选字典解码，1 对应于 `ABNR_REASON_GRP` 为 Steel Making，`APPRD_GRD` 为 合格，`APPR_FIN_MTH_CD` 为 冷矫直，`APPR_JDG_GRD` 为 合格。因此，1 的业务含义为“合格冷矫直”。由于有具体的字典解码对应，这个状态的含义较为明确。

#### 2. 流转与触发
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的流转顺序。但根据写入过程 `BSCPlanRsltConf` 的命名，可以推测该过程可能与计划结果确认相关。
- **触发过程**：状态 1 由 `BSCPlanRsltConf` 过程写入。

#### 3. 业务含义
- `WK_INST_YN` 字段在业务流程中的作用可能与工作指令的执行状态有关。具体来说，它可能表示某个工作指令（如钢制作）是否已经完成，并且是否符合质量标准。由于观测到的状态 1 表示“合格冷矫直”，这可能意味着在钢制作过程中，某个步骤（如冷矫直）已经完成，并且达到了质量要求。

#### 4. 不确定点
- **待确认**：由于只观测到一个状态写入，无法确定是否存在其他状态以及完整的状态流转顺序。
- **待确认**：`BSCPlanRsltConf` 过程的具体业务逻辑和触发条件未知，需要进一步的代码分析或业务文档来确认。
```

### 19. `SMS_SLAB.PROG_CD`

### 状态机理解

#### 1. **状态清单**
- **PH1C**：厚板作业等待（根据候选字典解码）

#### 2. **流转与触发**
- 根据观测到的状态写入，状态 `PH1C` 由过程 `BSC_WORK_INST_PLT` 写入。由于没有更多的状态写入证据，无法确定其他状态的流转顺序。
- **流转顺序**：未知（待确认）
- **触发过程**：
  - 状态 `PH1C` 由 `BSC_WORK_INST_PLT` 触发。

#### 3. **业务含义**
- 状态字段 `SMS_SLAB.PROG_CD` 记录了板坯在生产过程中的当前进度代码。具体来说，`PH1C` 表示板坯处于厚板作业等待阶段，这可能是生产流程中的一个特定环节，等待进一步的加工或处理。

#### 4. **不确定点**
- **流转顺序**：由于缺乏更多的状态写入证据，无法确定完整的状态流转顺序。需要进一步的证据来确认状态之间的流转关系。**待确认**
- **其他状态**：除了 `PH1C` 之外，可能还有其他状态存在，但由于缺乏证据，无法确定这些状态的具体值和业务含义。**待确认**

### 20. `SYD_DISP_ORD_DETAIL_BATCH.UPT_FLAG`

```markdown
### 状态字段的状态机理解

#### 1. 状态清单

| 值 | 业务含义（含不确定标注） |
| --- | --- |
| 1 | 根据候选字典解码，值为1可能代表“合格”状态，但具体业务含义需结合上下文进一步确认。**待确认** |

#### 2. 流转与触发

- **可能的状态流转顺序**：由于只观测到1状态的写入，无法确定完整的状态流转顺序。可能存在其他状态，但未观测到。
- **各转移由哪个/类过程触发（据写入包推断）**：
  - 状态1由`BSIF_MES_TO_ERP_RET`过程触发写入。

#### 3. 业务含义

该字段`SYD_DISP_ORD_DETAIL_BATCH.UPT_FLAG`在业务流程中的作用可能与订单批次的更新或状态标记有关。具体而言，它可能用于标识订单批次在某个业务环节（如生产、质量检查等）的状态变化。例如，当订单批次通过质量检查并被标记为“合格”时，该字段可能被设置为特定值以反映这一状态。

#### 4. 不确定点

- 由于只观测到一个状态的写入，无法确定完整的状态流转和所有可能的状态值。其他状态值及其业务含义需要进一步的证据来确认。**待确认**
- 状态1的具体业务含义仅根据候选字典解码推测，缺乏直接的业务上下文信息。**待确认**
- 该字段在业务流程中的具体作用和上下文也需要更多的业务知识来明确。**待确认**
```

### 21. `SYD_DISP_ORD_DETAIL_BATCH.READ_FLAG`

```markdown
### 状态机理解

#### 1. 状态清单
- **0**：根据候选字典解码，0 对应的业务含义可能为 `[AUTO_REFRESH] 选择日期`。但由于没有直接匹配的选项，这个解释可能不准确，需要进一步确认。因此，我们将其标记为 **[待确认]**。

#### 2. 流转与触发
- **0 ← BSIF_MES_TO_ERP_RET**：状态0是由过程 `BSIF_MES_TO_ERP_RET` 写入的。这表明在MES系统与ERP系统的数据交互过程中，可能会将状态设置为0。

#### 3. 业务含义
`SYD_DISP_ORD_DETAIL_BATCH.READ_FLAG` 字段在业务流程中的作用可能与订单详情批次的读取状态有关。具体来说，它可能用于标识某个订单详情批次是否已经被读取或处理过。状态0可能表示该批次尚未被读取或处理。

#### 4. 不确定点
- **状态0的业务含义**：由于候选字典解码中没有直接匹配的选项，状态0的具体业务含义 **[待确认]**。
```

### 22. `SMM_TRANSMISSION_ERP_SUM.CANCEL_FLAG`

```markdown
## 状态机理解

### 1. **状态清单**

| 值 | 业务含义（含不确定标注） |
| --- | --- |
| 9 | 不合格（来自[APPRD_GRD]、[APPR_JDG_GRD]、[CHEM_JDG_GRD]字典解码，但具体含义待确认） |

### 2. **流转与触发**

- **流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序。
- **触发过程**：
  - 状态9由`BSIF_MES_TO_ERP_RET`过程写入。

### 3. **业务含义**

`CANCEL_FLAG`字段在`SMM_TRANSMISSION_ERP_SUM`表中，根据字段名称和上下文推测，该字段可能用于标识与ERP系统交互过程中的取消或作废状态。具体而言，它可能用于记录和跟踪在MES系统与ERP系统数据传输过程中，哪些记录被标记为不合格或作废，从而影响后续的业务流程。

### 4. **不确定点**

1. **状态9的具体含义**：虽然从字典解码中推测状态9可能表示“不合格”，但由于缺乏更多上下文信息，无法确定这一状态在具体业务流程中的具体含义，需要进一步确认。
2. **完整的状态流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序，需要更多的状态写入证据来推断。
3. **其他可能的状态值**：除了观测到的状态9外，可能还有其他状态值存在，但由于缺乏证据，无法确定。
```

### 23. `SQM_SMP_LOTFORM_RSLT.REP_YN`

```markdown
### 1. 状态清单

- **N**：未接收（ACCEPT_YN）/ Not Check（ALLOC_CHK_TY）/ 未完成（APPR_JDG_YN）。由于候选字典解码提供了多个可能的含义，但根据状态写入的上下文，我们可以推断“未接收”是最符合业务逻辑的解释。因此，我们将其标记为“未接收”，同时标注“待确认”以表示需要进一步的证据来确认这一解释。

### 2. 流转与触发

- **流转顺序**：由于只观测到“N”状态的写入，没有其他状态的写入证据，我们无法确定完整的状态流转顺序。但是，我们可以推测“N”状态可能是初始状态或者在某些条件下重置的状态。
- **触发过程**：状态“N”由以下过程触发：
  - `BSQM_SMP_LOTFORM_RSLT`：可能负责初始化或重置状态。
  - `BSQM_SMP_LOTFORM_RSLT_GH1`、`BSQM_SMP_LOTFORM_RSLT_GH2`、`BSQM_SMP_LOTFORM_RSLT_GK`、`BSQM_SMP_LOTFORM_RSLT_HC`：这些过程可能在特定的业务逻辑下触发状态“N”的写入，例如在质保书或板坯处理的不同阶段。

### 3. 业务含义

- **REP_YN**字段在业务流程中的作用是标识质保书或板坯的某个处理结果是否已经被接收或处理。具体来说，它可能用于跟踪质保书的生成和处理状态，确保在产品发货前所有的质量检查和处理都已经完成。

### 4. 不确定点

- **状态解码**：由于候选字典解码提供了多个可能的含义，我们需要进一步的业务逻辑或上下文来确定“N”状态的确切含义。目前我们根据上下文推断为“未接收”，但这一解释需要“待确认”。
- **状态流转**：由于缺乏其他状态的写入证据，我们无法确定完整的状态流转顺序，这需要“待确认”。
- **触发过程的具体业务逻辑**：我们只知道哪些过程触发了“N”状态的写入，但具体的业务逻辑和条件需要进一步的分析和确认，这也需要“待确认”。
```

### 24. `SCH_SLAB_DESIGN_RESULT.DGN_MANUAL_FL`

```markdown
# SCH_SLAB_DESIGN_RESULT.DGN_MANUAL_FL 状态机理解

## 1. 状态清单
- **S**：Succeeded（成功），由 BSCH_BATCHA_PLT_JOB2 过程写入。这个状态表示在板坯设计结果中，手动干预或操作已经成功完成。

## 2. 流转与触发
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序。但可以推断，状态 S 可能是在板坯设计过程中的某个步骤被触发和设置的。
- **触发过程**：状态 S 由 BSCH_BATCHA_PLT_JOB2 过程触发。这可能意味着在板坯生产流程中的某个自动化或手动步骤中，当操作成功完成时，会将 DGN_MANUAL_FL 设置为 S。

## 3. 业务含义
- **字段作用**：DGN_MANUAL_FL 字段在业务流程中的作用可能是标识板坯设计结果是否需要或已经经过手动干预。在板坯的生命周期中，这个字段可能用于跟踪和记录设计过程中的关键步骤，确保设计结果的准确性和完整性。

## 4. 不确定点
- **状态解码**：由于只观测到一个状态写入，其他可能的状态及其业务含义无法确定。需要更多的上下文信息或状态写入证据来完整解码其他状态。
- **状态流转**：由于缺乏完整的状态流转证据，无法确定 DGN_MANUAL_FL 字段的所有可能状态及其流转顺序。这需要进一步的分析和证据收集。
- **业务上下文**：DGN_MANUAL_FL 字段在板坯设计过程中的具体作用和业务上下文需要进一步确认，特别是在没有更多状态写入证据的情况下。
- **待确认**：
  - 其他可能的状态及其业务含义。
  - DGN_MANUAL_FL 字段在板坯设计过程中的具体作用和业务上下文。
```

### 25. `SCH_SLAB_DESIGN_ORD.SLAB_DGN_NORMAL_YN`

```markdown
# SCH_SLAB_DESIGN_ORD.SLAB_DGN_NORMAL_YN 状态机理解

## 1. 状态清单
- **E**：根据候选字典解码，E 可能代表 'ALLOC_TY' 的 'Order Exchange'，但考虑到字段命名和上下文，'E' 更可能代表 'ANALYSIS PLACE CODE' 的 '低倍实验室' 或者 'ANAL_TY' 的 '炼钢化学成分改判率'。由于 'SLAB_DGN_NORMAL_YN' 字段名暗示了与设计或正常状态有关，'E' 可能表示某种异常或特殊状态。因此，'E' 的业务含义为 **异常或特殊设计状态**（待确认）。

## 2. 流转与触发
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序。但可以推断，状态 'E' 可能由正常状态流转而来，或者在特定条件下被设置。
- **触发过程**：状态 'E' 由 `BSCCH_BATCHA_PLT_JOB2` 过程触发写入。

## 3. 业务含义
`SCH_SLAB_DESIGN_ORD.SLAB_DGN_NORMAL_YN` 字段在业务流程中的作用可能与板坯的设计状态有关，特别是在质量控制和设计验证环节。该字段可能用于标识板坯是否符合设计规范，或者是否需要特殊的处理或分析。'E' 状态可能表示板坯在设计或生产过程中遇到了某种异常情况，需要特别的关注或处理。

## 4. 不确定点
- 字段名 `SLAB_DGN_NORMAL_YN` 暗示了与设计或正常状态有关，但具体的业务含义和状态流转需要更多的上下文信息来确认。
- 'E' 状态的具体业务含义和触发条件不明确，需要进一步的业务流程和字典信息来确定。
- 由于只观测到一个状态写入，无法确定完整的状态流转顺序和所有可能的状态。
```

### 26. `SPR_HEAT_WAIT.SLAB_STS_CD`

```markdown
## 状态字段状态机理解

### 1. **状态清单**

- **3**：根据候选字典解码，状态3的业务含义为「Hot Rolling」（热轧），属于「ABNR_REASON_GRP」类别。同时，该状态关联到「ALTER_PLT_CD」为3500产线，以及「APPR_FIN_MTH_CD」为修磨。由于候选字典解码中没有直接对应「BATTERY_TY」的解释，但考虑到其值「1+2 Battery」，可能与电池类型有关，因此这部分含义标为「待确认」。

### 2. **流转与触发**

- **流转顺序**：根据观测到的状态写入，状态3由`BSPR_HEAT_FCE_TEST`和`BSSM_HEAT_FCE`两个过程写入。由于缺乏更多的状态写入证据，无法确定完整的状态流转顺序。
- **触发过程**：状态3的写入由`BSPR_HEAT_FCE_TEST`和`BSSM_HEAT_FCE`两个过程触发。

### 3. **业务含义**

- `SPR_HEAT_WAIT.SLAB_STS_CD`字段在业务流程中的作用可能与板坯在热轧阶段的状态管理有关。具体来说，它记录了板坯在热轧过程中的状态，如是否进行热轧（状态3表示正在进行热轧），以及相关的产线信息和后续处理方法（如修磨）。

### 4. **不确定点**

- **状态3的`BATTERY_TY`含义**：由于候选字典解码中没有直接对应`BATTERY_TY`的解释，且值「1+2 Battery」的含义不明确，这部分业务含义需要进一步确认，标为「待确认」。
```

### 27. `SPR_CB_LOAD.PROCESS_FLAG`

```markdown
### 状态字段：SPR_CB_LOAD.PROCESS_FLAG

#### 1. 状态清单
- **1**：根据候选字典解码，此状态可能表示“合格”的钢材，且与“冷矫直”相关。由于解码信息有限，具体业务含义还需进一步确认。**[待确认]**

#### 2. 流转与触发
- **写入过程**：BSPR_OPER_L2 过程将 PROCESS_FLAG 设置为 1。
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序。可能存在其他状态和过程，但当前证据不足。

#### 3. 业务含义
- SPR_CB_LOAD.PROCESS_FLAG 字段在业务流程中的作用可能与钢材的加工和质量控制相关。具体来说，它可能用于标识钢材在冷矫直过程中的质量状态，如是否合格。这个字段可能在整个生产过程中跟踪钢材的质量，确保只有合格的钢材才能进入下一个生产环节。

#### 4. 不确定点
- **状态含义**：由于只观测到一个状态值，且候选字典解码信息有限，无法完全确定状态 1 的具体业务含义。需要更多的上下文信息和业务知识来准确解释这个状态。**[待确认]**
- **状态流转**：由于缺乏其他状态和过程的写入证据，无法确定完整的状态流转顺序和触发条件。**[待确认]**
```

### 28. `SPR_MARK_INFO.PROCESS_FLAG`

```markdown
### 1. 状态清单

| 值 | 业务含义 |
| --- | --- |
| 1 | 根据候选字典解码，1 对应于 [ABNR_REASON_GRP] 炼钢；[APPRD_GRD] 合格；[APPR_FIN_MTH_CD] 冷矫直；[APPR_JDG_GRD] 合格。因此，我们可以推断状态1表示“炼钢合格，冷矫直，合格判断”。 |

### 2. 流转与触发

- **状态流转**：由于只观测到状态1由BSPR_OPER_L2过程写入，没有其他状态的写入证据，因此无法确定完整的状态流转顺序。可能的状态流转包括从其他状态到状态1，或从状态1到其他状态。
- **触发过程**：状态1由BSPR_OPER_L2过程触发写入。

### 3. 业务含义

`SPR_MARK_INFO.PROCESS_FLAG`字段在业务流程中的作用可能与钢材的加工和质量控制相关。具体来说，它可能用于标识钢材在炼钢、矫直等加工过程中的状态，以及质量是否合格。这个字段有助于跟踪钢材的加工历史和质量状态，对于确保产品质量和满足客户要求至关重要。

### 4. 不确定点

- 由于只观测到状态1的写入，其他状态的存在和流转顺序「待确认」。
- 状态1的具体含义和业务流程中的确切作用「待确认」，需要更多的上下文信息和业务知识来确定。
```

### 29. `SPR_XM_DEFECT_LIST.LAST_FL`

```markdown
## 状态字段：SPR_XM_DEFECT_LIST.LAST_FL

### 1. **状态清单**
- **1**：合格（根据字典解码推断）

### 2. **流转与触发**
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的状态流转顺序。
- **触发过程**：状态1由`BSPR_APPR_JUDGE_RSLT`过程触发。

### 3. **业务含义**
该字段`LAST_FL`在业务流程中的作用是标识板坯在钢厂生产过程中的最终质量判断结果。具体来说，它记录了板坯在经过冷矫直等工序后的质量状态，是板坯质量控制的重要环节。通过这个字段，可以快速了解板坯是否达到了质量标准，对于后续的加工、销售等环节具有指导意义。

### 4. **不确定点**
- **状态值的完整性**：由于只观测到一个状态值，无法确定是否存在其他状态值，需要进一步的证据来确认完整的状态清单。
- **状态流转的完整性**：由于缺乏更多的状态写入证据，无法确定完整的状态流转顺序，需要更多的过程调用信息来推断。
- **状态值与业务含义的对应关系**：虽然根据字典解码推断出状态1为“合格”，但由于缺乏更多的上下文信息，这个对应关系仍需进一步确认。
```

### 30. `SPR_PLATE.HTM_INST_REQ_YN`

```markdown
## 状态字段：SPR_PLATE.HTM_INST_REQ_YN

### 1. 状态清单
- **Y**：已接收（根据候选字典解码中的[ACCEPT_YN]已接收，选择最贴切的业务含义）

### 2. 流转与触发
- **流转**：目前只有从未知状态到Y（已接收）的流转证据。
- **触发**：由`BSPR_BUND_OBOUT`过程触发状态写入为Y。

### 3. 业务含义
该字段`SPR_PLATE.HTM_INST_REQ_YN`在业务流程中的作用是标识板坯是否已经接收了热处理的指令。具体来说，当`BSPR_BUND_OBOUT`过程执行时，会将该字段设置为Y，表示板坯已经接收到了热处理的指令，这是板坯生命周期中的一个重要环节，涉及到生产调度和质量控制。

### 4. 不确定点
- 由于只观测到一个过程写入Y状态，对于其他可能的状态（如N或空值）及其流转情况「待确认」。
- 候选字典解码中提供了多个可能的含义，但选择了“已接收”作为Y状态的业务含义，这一选择「待确认」，因为缺乏更多的上下文信息来确定其他含义是否适用。
- 该字段的具体业务流程环节和作用「待确认」，因为缺乏关于板坯生命周期的详细流程描述。
```

### 31. `SPR_SPRB0330.USE_YN`

```markdown
## 状态字段：SPR_SPRB0330.USE_YN

### 1. 状态清单
- **N**：未接收（根据候选字典解码，[ACCEPT_YN] 未接收，[ALLOC_CHK_TY] Not Check，[APPR_JDG_YN] 未完成，结合字段名和上下文推测为“未使用”状态）

### 2. 流转与触发
- **流转顺序**：由于只观测到一个状态写入，无法确定完整的流转顺序。
- **触发过程**：状态 N 由 `BSPR_CUTTING_SEARCH` 过程写入。

### 3. 业务含义
该字段 `SPR_SPRB0330.USE_YN` 表示板坯是否被使用。在板坯的生命周期管理中，这个字段用于标识板坯是否已经被切割或分配到具体的生产订单中。"N" 状态可能表示板坯尚未被使用，处于待分配状态。

### 4. 不确定点
- 由于只观测到一个状态写入，无法确定其他可能的状态及其流转顺序，需要更多的状态写入证据来完善状态机。
- 状态 "N" 的具体业务含义为“未使用”，但缺乏直接的字典解码支持，因此这个解释需要进一步确认。【待确认】
```

### 32. `SMS_HEAT.HEAT_STS`

```markdown
### 1. 状态清单

| 值 | 业务含义（含不确定标注） |
|---|---|
| 2 | Casting（浇铸）; 订单外一级; 压力矫; 订单外1级（不确定标注） |

### 2. 流转与触发

- **可能的状态流转顺序**：由于只观测到状态2的写入，无法确定完整的状态流转顺序。
- **各转移由哪个/类过程触发**：
  - 状态2由`BSMS_OPER_CC`过程写入。

### 3. 业务含义

`SMS_HEAT.HEAT_STS`字段代表的是钢厂中一个炉次（Heat）的状态。在钢厂的生产流程中，每个炉次都会经历不同的状态，这些状态反映了炉次在生产过程中的不同阶段。该字段在业务流程中的作用是标识当前炉次所处的具体状态，这对于生产调度、质量控制和库存管理等环节至关重要。

### 4. 不确定点

- 状态2的业务含义中包含了多个可能的解释，如"Casting（浇铸）"、"订单外一级"、"压力矫"和"订单外1级"。由于缺乏更多的上下文信息，无法确定这些含义中哪一个或哪几个是正确的，因此标注为「待确认」。
- 由于只观测到一个状态的写入，无法确定状态机的完整流转路径，这也需要进一步的「待确认」。
```

### 33. `SMS_HEAT.LST_CUT_YN`

```markdown
## 1. **状态清单**

| 值 | 业务含义 |
| --- | --- |
| N | 未接收（根据候选字典解码[ACCEPT_YN]推断） |

## 2. **流转与触发**

- **流转顺序**：由于只观测到一个状态写入，无法确定完整的流转顺序。
- **触发过程**：状态写入由`BSMS_OPER_CC`过程触发，将`LST_CUT_YN`字段的值设置为`N`。

## 3. **业务含义**

`LST_CUT_YN`字段表示的是某个操作是否已经完成切割的标记。在业务流程中，这个字段可能用于跟踪和管理板坯或工单在生产过程中的切割状态，特别是在热处理环节。当`LST_CUT_YN`为`N`时，表示相关的板坯或工单尚未完成切割，这可能是在生产调度、质量控制或库存管理中的一个重要参考。

## 4. **不确定点**

- **状态值的完整范围**：由于只观测到`N`这一个状态值，不确定是否存在其他状态值，如`Y`（表示已接收或已完成切割）。
- **状态流转的完整性**：由于缺乏更多的状态写入证据，无法确定`LST_CUT_YN`字段的完整流转路径。
- **业务上下文**：`LST_CUT_YN`字段的具体业务含义和它在生产流程中的作用需要更多的业务上下文信息来确认，目前只能根据字段名称和候选字典解码进行推测。
- **待确认**：由于缺乏完整的状态写入证据和业务流程信息，上述分析可能存在偏差，需要进一步的业务确认和数据支持。
```

### 34. `SMS_RSLT_CUT.LST_SLAB_YN`

```markdown
### 1. 状态清单

- **N**：未接收（根据候选字典解码[ACCEPT_YN]未接收推测得出）
- **Y**：待确认（没有足够的信息来确定Y对应的具体业务含义）

### 2. 流转与触发

- **流转顺序**：由于只观测到N状态的写入，无法确定完整的状态流转顺序。
- **触发过程**：状态N由`BSMS_OPER_CC`过程写入。

### 3. 业务含义

`SMS_RSLT_CUT.LST_SLAB_YN`字段似乎是用来标识板坯是否已经被切割。在MES系统中，这个字段可能用于跟踪板坯的加工状态，特别是在板坯切割这一步骤。如果字段值为N，可能表示板坯尚未被切割；如果值为Y，则可能表示板坯已经被切割。这个字段对于监控板坯的加工进度和质量控制至关重要。

### 4. 不确定点

- **Y状态的含义**：由于没有观测到Y状态的写入，其具体业务含义「待确认」。
- **状态流转完整性**：由于只观测到一个状态的写入，无法确定所有可能的状态流转，这部分信息「待确认」。
```

---

<!-- T3-3-2-TRACEABILITY -->

## 第二部分：批次/卷/试样追溯链路（T3-3-2）

| 项 | 内容 |
|----|----|
| 需求单 | REQ-MES-AI-20260715-001（T3-3-2）|
| 日期 | 2026-07-15 |
| 追溯边（有证据）| 13 |
| Token | 21,650 |
| 验收目标 | 追溯链路描述准确率 ≥90%（BIZ 复核）|

> 追溯链路以**表结构（同表共键=血缘桥表）+ P0 过程读写血缘**为证据反推 + AI 合成；**须 BIZ 对照真实工艺流复核**（尤其无桥表/P0 未覆盖处的「待确认」跳）。

### 核心实体载体画像

| 实体 | 键列 | 承载表数 | 主表数 | 主表示例 | 子系统分布 |
|----|----|----|----|----|----|
| 订单 | `ORD_NO` | 189 | 38 | SCH_PASS_PLANT_RESULT_ORD, SCH_PPC_PLAN_ORD, SCH_PPC_PLAN_ORDT | SCH×60, SQM×51, SYD×15, SMS×13, SPR×12, SPG×10 |
| 计划炉次 | `PLAN_HEAT_NO` | 62 | 18 | SCH_CC_NO_CHG_LOG, SCH_CC_WORK_DETAIL_INFO, SCH_HEAT_SCHEDULE | SCH×27, SMS×19, SIF×10, SQM×5, SCK×1 |
| 浇铸(连铸) | `CAST_NO` | 25 | 3 | SCH_CAST_START_TIME, SCH_STANDARD_STLLEN, SMS_RSLT_TUNDISH_OPERATE | SCH×11, SMS×7, SIF×6, SCK×1 |
| 炉次 | `HEAT_NO` | 176 | 44 | SCH_HEAT_RETURN, SCH_PLAN_BATCH_GB, SCH_ROLL_BATCH_GB | SMS×60, SQM×25, SCH×23, SIF×19, SWR×14, SYD×7 |
| 板坯 | `SLAB_NO` | 96 | 32 | SCH_HSM_ROLL_DESIGN_SLAB, SCH_HSM_ROLL_DESIGN_SLABT, SHR_FCE_UNCHARGE | SPR×30, SCH×23, SQM×11, SMS×10, SHR×9, SYD×4 |
| 钢卷 | `COIL_NO` | 47 | 32 | SCH_COLD_WORK_DESIGN_COIL, SCH_COLD_WORK_DESIGN_COILT, SCH_SCH_CRL | SCR×25, SHR×13, SCH×4, SMS×3, SQM×2 |
| 热卷 | `HCOIL_NO` | 18 | 7 | SCH_HOT_REVISE_PLAN_COIL, SCH_HOT_REVISE_PLAN_COILT, SCH_PLTCM_WORK_PLAN_COIL | SCH×7, SCR×4, SYD×3, SPR×1, TEST×1, SSD×1 |
| 钢板(成品板) | `PLT_NO` | 95 | 63 | SCH_PLAN_PLATE, SPR_ACC_RSLT, SPR_ACC_RSLT_BK | SPR×84, SSM×9, SCH×2 |
| 试样 | `SMP_NO` | 90 | 15 | SPR_MECH_NOTE_LIST, SQM_CC_MECH_RSLT, SQM_HC_MECH_RSLT | SQM×41, SCH×24, SPR×6, SYD×3, SMS×3, TB×3 |
| 质保/材质 | `MTC_NO` | 21 | 6 | SQM_MTC_CHEM, SQM_MTC_COM, SQM_MTC_MECH | SQM×21 |
| 捆包 | `BUND_NO` | 11 | 4 | SPR_BUND_RSLT, SPR_HTM_BUND, SPR_MAX_BUND | SPR×7, SSM×2, SWR×2 |
| 批次 | `LOT_NO` | 51 | 0 | — | SQM×30, SPR×8, TB×5, SIM×3, SHR×1, SCR×1 |
| 产品 | `PROD_NO` | 32 | 17 | SQM_JDG_HLD, SQM_JDG_HLD_HIST, SQM_JDG_SHP_ORG | SYD×15, SQM×14, SSD×1, TEST×1, SPG×1 |
| 物料 | `MTL_NO` | 24 | 13 | SCH_HSM_WORK_DETAIL_INFO, SCH_INST_ROLL, SCH_INST_ROLL_TEST | SCH×15, SPR×6, SSM×2, SQM×1 |

### 追溯边一览

| 上游 | 下游 | 桥接表数 | 桥接过程数 |
|----|----|----|----|
| 订单(`ORD_NO`) | 计划炉次(`PLAN_HEAT_NO`) | 28 | 20 |
| 计划炉次(`PLAN_HEAT_NO`) | 炉次(`HEAT_NO`) | 36 | 22 |
| 浇铸(连铸)(`CAST_NO`) | 炉次(`HEAT_NO`) | 12 | 14 |
| 炉次(`HEAT_NO`) | 板坯(`SLAB_NO`) | 47 | 26 |
| 板坯(`SLAB_NO`) | 钢板(成品板)(`PLT_NO`) | 34 | 19 |
| 板坯(`SLAB_NO`) | 钢卷(`COIL_NO`) | 10 | 16 |
| 钢卷(`COIL_NO`) | 热卷(`HCOIL_NO`) | 5 | 7 |
| 钢板(成品板)(`PLT_NO`) | 试样(`SMP_NO`) | 8 | 17 |
| 板坯(`SLAB_NO`) | 试样(`SMP_NO`) | 40 | 25 |
| 试样(`SMP_NO`) | 质保/材质(`MTC_NO`) | 12 | 8 |
| 钢板(成品板)(`PLT_NO`) | 质保/材质(`MTC_NO`) | 0 | 1 |
| 钢板(成品板)(`PLT_NO`) | 捆包(`BUND_NO`) | 7 | 15 |
| 订单(`ORD_NO`) | 质保/材质(`MTC_NO`) | 9 | 8 |

---

### 追溯边链路明细

#### 1. 订单（`ORD_NO`）→ 计划炉次（`PLAN_HEAT_NO`）

### 追溯边的链路理解

#### 1. **正向追溯**：由上游 订单 如何定位其下游 计划炉次

- **桥接表**：根据提供的信息，`SCH_PLAN_HEAT` 表同时包含 `ORD_NO` 和 `PLAN_HEAT_NO`，可以作为订单和计划炉次之间的直接桥接表。这意味着订单通过 `ORD_NO` 键与 `SCH_PLAN_HEAT` 表关联，进而通过 `PLAN_HEAT_NO` 键与计划炉次关联。

- **桥接过程**：`BSCCH_PLAN_ADJUST` 过程从 `SCH_PLAN_HEAT` 和 `SCH_PLAN_ROLL` 读取数据，并写入 `SCH_PLAN_HEAT` 和 `SCH_PLAN_SLAB`。这表明订单信息可能通过调整过程被传递到计划炉次。

#### 2. **反向追溯**：由 计划炉次 如何回溯到 订单

- **桥接表**：同样，`SCH_PLAN_HEAT` 表也可以用来从计划炉次回溯到订单，因为该表包含 `ORD_NO` 和 `PLAN_HEAT_NO`。

- **桥接过程**：`BSCCH_PLAN_ADJUST` 过程也可以用来从计划炉次回溯到订单，因为它从 `SCH_PLAN_HEAT` 读取数据，而 `SCH_PLAN_HEAT` 包含 `ORD_NO`。

#### 3. **关键桥接点**

- **表**：`SCH_PLAN_HEAT` 是关键的桥接表，因为它同时包含订单号 `ORD_NO` 和计划炉次号 `PLAN_HEAT_NO`。在业务流程中，这个表可能用于记录和跟踪订单与计划炉次之间的对应关系。

- **过程**：`BSCCH_PLAN_ADJUST` 是关键的桥接过程，因为它涉及从包含订单信息的表读取数据，并写入包含计划炉次信息的表。这个过程在订单和计划炉次之间的数据传递中扮演重要角色。

#### 4. **断点与不确定点**

- **桥接表的完整性**：虽然 `SCH_PLAN_HEAT` 表被提及为桥接表，但没有提供关于该表是否总是包含所有必要的 `ORD_NO` 和 `PLAN_HEAT_NO` 信息的详细信息。这需要进一步确认，以确保追溯链的完整性。

- **桥接过程的覆盖**：提供的桥接过程列表可能不完整，可能还有其他过程涉及订单和计划炉次之间的数据传递。这需要进一步的确认和分析。

- **动态SQL和未覆盖的过程**：如果存在动态SQL或未在P0存储过程中覆盖的其他过程，这可能会影响追溯链的完整性。这些情况需要特别标记为「待确认」。

综上所述，虽然我们可以根据提供的桥接表和过程推断出订单和计划炉次之间的追溯链路，但仍有一些关键点需要进一步确认和验证。

#### 2. 计划炉次（`PLAN_HEAT_NO`）→ 炉次（`HEAT_NO`）

以下是对追溯边“计划炉次（键 PLAN_HEAT_NO）→ 炉次（键 HEAT_NO）”的链路理解：

### 1. 正向追溯：由上游 计划炉次 如何定位其下游 炉次

- **桥接表**：根据提供的桥接表信息，`SCH_ROLL_BATCH_SLAB_GB`、`SCH_ROLL_BATCH_SLAB_GB_OLD`、`SCH_ROLL_BATCH_SLAB_GB_OLD1`、`SCH_ROLL_BATCH_SLAB_GB_OLD_OLD` 这些表同时包含 `PLAN_HEAT_NO` 和 `HEAT_NO`，表明它们可以作为从计划炉次到炉次的直接追溯桥梁。这些表可能记录了计划炉次与实际炉次之间的对应关系。

- **桥接过程**：`BSMS_OPER_BOF` 过程读取了 `SCH_INST_HEAT` 和 `SMS_HEAT` 表，并写入 `SMS_MAT_CONSUM`、`SMS_RSLT_BB_BTM`、`SMS_RSLT_BOF` 表。由于 `SCH_INST_HEAT` 表包含 `HEAT_NO`，而 `SCH_PLAN_HEAT` 表包含 `PLAN_HEAT_NO`，这个过程可能涉及到从计划炉次到炉次的转换。

### 2. 反向追溯：由炉次如何回溯到计划炉次

- **桥接表**：`SCH_HEAT_RETURN` 表包含 `HEAT_NO` 作为主键，同时也包含 `PLAN_HEAT_NO`，因此可以用来从炉次回溯到计划炉次。

- **桥接过程**：`BSMS_OPER_BOF` 过程读取了 `SCH_INST_HEAT` 和 `SMS_HEAT` 表，并写入 `SMS_MAT_CONSUM`、`SMS_RSLT_BB_BTM`、`SMS_RSLT_BOF` 表。由于 `SCH_INST_HEAT` 表包含 `HEAT_NO`，而 `SCH_PLAN_HEAT` 表包含 `PLAN_HEAT_NO`，这个过程可能涉及到从炉次到计划炉次的追溯。

### 3. 关键桥接点

- **表**：`SCH_HEAT_RETURN` 是一个关键的桥接表，因为它直接关联了 `HEAT_NO` 和 `PLAN_HEAT_NO`，可能记录了炉次的返回信息，包括计划炉次和实际炉次的对应关系。
  
- **过程**：`BSMS_OPER_BOF` 是一个关键的桥接过程，因为它涉及到从计划炉次到炉次的转换，并且可能包含相关的生产操作信息。

### 4. 断点与不确定点

- **桥接表**：`SCH_ROLL_BATCH_SLAB_GB` 系列表虽然包含 `PLAN_HEAT_NO` 和 `HEAT_NO`，但它们的具体业务角色和数据完整性需要进一步确认，标记为「待确认」。

- **桥接过程**：除了 `BSMS_OPER_BOF` 过程外，其他过程是否涉及计划炉次到炉次的转换需要进一步分析，标记为「待确认」。

- **动态 SQL**：如果存在动态 SQL 或未覆盖的过程，可能会影响追溯链路的完整性，需要进一步确认，标记为「待确认」。

#### 3. 浇铸(连铸)（`CAST_NO`）→ 炉次（`HEAT_NO`）

### 追溯边链路理解

#### 1. 正向追溯：由上游 浇铸(连铸) 如何定位其下游 炉次

- **桥接表**：
  - `SCK_RSLT_HEAT`：包含 `HEAT_NO` 主键，可能与 `CAST_NO` 相关联。
  - `SIF_CCM_LADLE_TRACK` 和 `SIF_CCM_RSLT`：可能包含 `CAST_NO` 和 `HEAT_NO`，用于跟踪炉次和连铸过程。
  - `SIF_HEAT_PLAN` 系列表：涉及炉次计划，可能与连铸相关。
  - `SMS_HEAT` 系列表：包含 `HEAT_NO` 主键，可能与 `CAST_NO` 相关联。

- **桥接过程**：
  - `BSMS_A5310`：读取 `SCH_INST_HEAT`, `SMS_HEAT`, `SMS_RSLT_HEAT` 并写入 `SMS_HEAT`, `SMS_RSLT_BOF`, `SMS_RSLT_CUT`，可能涉及从连铸到炉次的追溯。
  - `BSMS_OPER_BOF` 和 `BSMS_OPER_CC`：读取 `SCH_INST_HEAT`, `SMS_HEAT`, `SMS_RSLT_HEAT` 并写入相关结果表，可能涉及连铸到炉次的追溯。

#### 2. 反向追溯：由炉次如何回溯到浇铸(连铸)

- **桥接表**：
  - 同上，`SCK_RSLT_HEAT`, `SIF_CCM_LADLE_TRACK`, `SIF_CCM_RSLT`, `SIF_HEAT_PLAN` 系列表，`SMS_HEAT` 系列表。

- **桥接过程**：
  - 同上，`BSMS_A5310`, `BSMS_OPER_BOF`, `BSMS_OPER_CC` 等过程可能涉及从炉次回溯到连铸的信息。

#### 3. 关键桥接点

- **表**：
  - `SMS_HEAT`：作为 `HEAT_NO` 的主表，可能包含与 `CAST_NO` 相关的信息，是连接炉次和连铸的关键桥接点。

- **过程**：
  - `BSMS_A5310`：涉及从连铸到炉次的多个关键步骤，是追溯链中的重要过程。

#### 4. 断点与不确定点

- **断点**：
  - 桥接表和过程之间的具体关系需要进一步确认，特别是 `CAST_NO` 和 `HEAT_NO` 如何在这些表和过程中被具体使用和关联。

- **不确定点**：
  - `SIF_CCM_LADLE_TRACK` 和 `SIF_CCM_RSLT` 表的具体内容和它们与 `CAST_NO` 和 `HEAT_NO` 的关系需要进一步确认。
  - `SIF_HEAT_PLAN` 系列表的具体作用和它们在追溯链中的位置需要进一步确认。
  - 所有桥接过程的具体逻辑和它们如何使用 `CAST_NO` 和 `HEAT_NO` 需要进一步确认。
  - 其他未提及的过程可能也涉及 `CAST_NO` 和 `HEAT_NO` 的追溯，需要进一步分析和确认。

#### 4. 炉次（`HEAT_NO`）→ 板坯（`SLAB_NO`）

### 追溯边的链路理解

#### 1. **正向追溯**：由上游 炉次 如何定位其下游 板坯

- **桥接表**：`SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 同时包含 `HEAT_NO` 和 `SLAB_NO`，表明它们是炉次和板坯之间的直接关联表。`SCH_PLAN_PLATE` 和 `SCH_PLTCM_WORK_PLAN_COIL` 也包含这两个键，可能用于生产计划和工作计划中炉次与板坯的关联。
- **桥接过程**：`BSMS_OPER_HEAT` 过程读取 `SMS_HEAT` 和 `SMS_MAT_CONSUM`，写入 `SMS_SLAB`，表明它可能在炉次和板坯之间建立了联系。`BSMS_OPER_SUBDIV_CUT` 过程读取 `SCH_PLT_PLAN_ROLL` 和 `SMS_HEAT`，写入 `SMS_MSLAB` 和 `SMS_SLAB`，这也可能涉及到炉次到板坯的追溯。

#### 2. **反向追溯**：由 板坯 如何回溯到 炉次

- **桥接表**：`SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 作为板坯的主表，包含 `SLAB_NO` 并且与 `HEAT_NO` 相关联，可以通过这些表回溯到炉次。
- **桥接过程**：`BSMS_OPER_SUBDIV_CUT` 过程从 `SCH_PLT_PLAN_ROLL` 和 `SMS_HEAT` 读取数据并写入 `SMS_MSLAB` 和 `SMS_SLAB`，这表明它可能用于从板坯追溯到炉次。

#### 3. **关键桥接点**

- **表**：`SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 是关键的桥接表，它们作为板坯的主表，直接关联炉次和板坯，承担着炉次到板坯追溯的核心角色。
- **过程**：`BSMS_OPER_HEAT` 和 `BSMS_OPER_SUBDIV_CUT` 是关键的桥接过程，它们在炉次和板坯之间传递信息，是追溯链中的重要环节。

#### 4. **断点与不确定点**

- **桥接表**：虽然 `SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 包含 `HEAT_NO` 和 `SLAB_NO`，但没有具体的业务流程描述，因此它们在追溯链中的具体作用「待确认」。
- **桥接过程**：`BSMS_OPER_HEAT` 和 `BSMS_OPER_SUBDIV_CUT` 过程虽然涉及 `HEAT_NO` 和 `SLAB_NO` 的读写，但具体如何操作这些键值对以实现追溯「待确认」。
- **其他过程**：列表中提到的其他过程，如 `BSCM_B0025` 系列和 `BSCM_PLAN_ADJUST`，虽然涉及相关表的读写，但没有明确说明它们是否直接参与炉次到板坯的追溯，因此它们的具体作用「待确认」。

#### 5. 板坯（`SLAB_NO`）→ 钢板(成品板)（`PLT_NO`）

### 追溯边链路理解

#### 1. 正向追溯：由上游 板坯 如何定位其下游 钢板(成品板)

- **桥接表**：`SPR_COMPLEX_SLAB` 和 `SPR_HEAT_FCE_RSLT` 同时包含 `SLAB_NO` 和 `PLT_NO`，表明它们可以作为板坯到钢板的直接桥接表。
- **桥接过程**：`BSPR_HEAT_FCE` 过程读取 `SMS_SLAB`（板坯信息）和 `SCH_PLT_INST_ROLL`（卷信息），然后写入 `SPR_FCE_CHARGE`（板坯充电信息），这个过程中 `SLAB_NO` 和 `PLT_NO` 被关联起来，表明板坯通过加热过程转化为钢板。

#### 2. 反向追溯：由 钢板(成品板) 如何回溯到 板坯

- **桥接表**：`SCH_PLAN_PLATE` 和 `SPR_CUTTING_RSLT` 同时包含 `PLT_NO` 和 `SLAB_NO`，表明它们可以作为钢板到板坯的直接桥接表。
- **桥接过程**：`BSPR_CUTTING_RSLT` 过程读取 `SCH_PLAN_PLATE`（钢板计划）和 `SCH_PLT_INST_ROLL`（卷信息），然后写入 `SPR_CUT_CS_DTL_RSLT` 和 `SPR_CUT_CS_SUB_RSLT`（切割结果），这个过程中 `PLT_NO` 和 `SLAB_NO` 被关联起来，表明钢板通过切割过程可以追溯到板坯。

#### 3. 关键桥接点

- **桥接表**：`SPR_COMPLEX_SLAB` 和 `SPR_HEAT_FCE_RSLT` 是关键桥接表，它们直接关联了板坯和钢板的信息，承载了从板坯到钢板的血缘关系。
  - **业务角色**：`SPR_COMPLEX_SLAB` 可能记录了板坯的复杂属性和处理结果，而 `SPR_HEAT_FCE_RSLT` 记录了加热过程中板坯的结果，这两个表是板坯转化为钢板过程中的关键数据记录点。
- **桥接过程**：`BSPR_HEAT_FCE` 是关键桥接过程，它不仅关联了板坯和钢板的信息，还涉及到板坯的加热处理，是板坯转化为钢板的重要环节。
  - **业务角色**：`BSPR_HEAT_FCE` 过程负责处理板坯的加热，这是将板坯转化为钢板的关键工艺步骤，因此这个过程中的数据流转对于追溯链路至关重要。

#### 4. 断点与不确定点

- **桥接表**：虽然 `SPR_COMPLEX_SLAB` 和 `SPR_HEAT_FCE_RSLT` 同时包含 `SLAB_NO` 和 `PLT_NO`，但没有具体的业务逻辑描述，无法确定它们在追溯链路中的具体作用和准确性，**待确认**。
- **桥接过程**：`BSPR_HEAT_FCE` 过程中虽然涉及 `SLAB_NO` 和 `PLT_NO` 的读写，但没有具体的 SQL 语句或业务逻辑描述，无法确定其在追溯链路中的准确性和完整性，**待确认**。
- **其他过程**：`BSCH_B0025`、`BSCH_B0025S` 和 `BSCH_B0025_HEAT_NO` 等过程中虽然涉及 `SLAB_NO` 和 `PLT_NO` 的读写，但没有具体的业务逻辑描述，无法确定它们在追溯链路中的具体作用和准确性，**待确认**。

#### 6. 板坯（`SLAB_NO`）→ 钢卷（`COIL_NO`）

### 追溯边链路理解

#### 1. 正向追溯：由上游 板坯 如何定位其下游 钢卷

- **桥接表**：
  - `SHR_SLAB_HCOIL` 表同时包含 `SLAB_NO` 和 `COIL_NO`，表明该表是板坯和钢卷之间的直接关联表，可以作为正向追溯的关键证据。

- **桥接过程**：
  - `BSMS_OPER_SUBDIV_CUT` 过程读取 `SCH_PLAN_ROLL`, `SCH_PLT_PLAN_ROLL`, `SHR_HCOIL_MASTER` 并写入 `SMS_MSLAB`, `SMS_SLAB`。虽然该过程涉及 `SHR_HCOIL_MASTER`（包含 `COIL_NO`），但它主要操作的是 `SMS_SLAB`（包含 `SLAB_NO`），因此可以作为正向追溯的一个间接证据。

#### 2. 反向追溯：由 钢卷 如何回溯到 板坯

- **桥接表**：
  - 同正向追溯，`SHR_SLAB_HCOIL` 表是反向追溯的关键，因为它同时包含 `SLAB_NO` 和 `COIL_NO`。

- **桥接过程**：
  - `BSQM_SMP_LOTFORM_RSLT` 和 `BSQM_SMP_LOTFORM_RSLT_GH1` 过程读取 `SCH_PLAN_ROLL`, `SCH_PLT_INST_ROLL`, `SHR_HCOIL_MASTER` 并写入 `SCR_COIL_MASTER`, `SHR_HCOIL_MASTER`。这些过程涉及 `SHR_HCOIL_MASTER`（包含 `COIL_NO`），可以作为反向追溯的间接证据。

#### 3. 关键桥接点

- **表**：
  - `SHR_SLAB_HCOIL` 是最能承载板坯到钢卷血缘的表，因为它直接关联了 `SLAB_NO` 和 `COIL_NO`。在业务上，这个表可能用于记录板坯和钢卷之间的对应关系，是追溯链路的核心。

- **过程**：
  - `BSMS_OPER_SUBDIV_CUT` 和 `BSQM_SMP_LOTFORM_RSLT` 系列过程是关键的桥接过程，因为它们涉及板坯和钢卷的转换和记录。这些过程在业务上可能负责将板坯加工成钢卷，并记录这一过程的数据。

#### 4. 断点与不确定点

- **桥接表**：
  - 除了 `SHR_SLAB_HCOIL` 外，其他表如 `SHR_FCE_UNCHARGE`, `SHR_HCOIL_FCE_RSLT`, `SMS_MSLAB`, `SMS_SLAB`, `SMS_SLAB_BY_HEAT` 虽然包含 `SLAB_NO`，但未明确显示与 `COIL_NO` 的直接关联，因此其追溯关系需要进一步确认。

- **桥接过程**：
  - `BSMS_OPER_SUBDIV_CUT` 和 `BSQM_SMP_LOTFORM_RSLT` 系列过程虽然涉及 `SHR_HCOIL_MASTER`，但具体如何操作 `SLAB_NO` 和 `COIL_NO` 的关系需要进一步确认。
  - 其他未列出的过程可能也涉及板坯和钢卷的追溯，但由于信息不足，其具体作用和追溯关系需要进一步确认。

以上分析基于提供的静态证据，实际业务逻辑可能更复杂，需要结合业务知识和动态数据进一步验证。

#### 7. 钢卷（`COIL_NO`）→ 热卷（`HCOIL_NO`）

```markdown
# 追溯链路理解

## 1. 正向追溯：由上游 钢卷 如何定位其下游 热卷

正向追溯链路主要依赖于以下桥接表和桥接过程：

- **桥接表**：
  - `SCH_SCH_CRL`：包含`COIL_NO`作为主表键，同时包含`HCOIL_NO`。
  - `SCR_CC_HC_LNK_MGT`：包含`COIL_NO`作为主表键，同时包含`HCOIL_NO`作为主表键。
  - `SCR_COIL_MASTER`：包含`COIL_NO`作为主表键。

- **桥接过程**：
  - `BSQM_SMP_LOTFORM_RSLT`系列过程：读取`SCH_SCH_CRL`, `SCR_COIL_MASTER`, `SHR_HCOIL_MASTER`，并写入`SCR_COIL_MASTER`。这些过程通过读取包含`COIL_NO`和`HCOIL_NO`的表，并将信息写回`SCR_COIL_MASTER`，实现了从钢卷到热卷的正向追溯。

## 2. 反向追溯：由 热卷 如何回溯到 钢卷

反向追溯链路主要依赖于以下桥接表和桥接过程：

- **桥接表**：
  - `SCR_CC_HC_LNK_MGT`：包含`HCOIL_NO`作为主表键，同时包含`COIL_NO`作为主表键，提供了从热卷回溯到钢卷的直接路径。

- **桥接过程**：
  - 由于提供的桥接过程中没有直接从热卷写入钢卷的过程，反向追溯可能需要依赖于`SCR_CC_HC_LNK_MGT`表中的`COIL_NO`和`HCOIL_NO`字段，通过查询该表实现从热卷到钢卷的回溯。

## 3. 关键桥接点

- **关键桥接表**：`SCR_CC_HC_LNK_MGT`
  - 业务角色：作为直接连接钢卷和热卷的桥梁，存储了两者之间的对应关系，是实现正向和反向追溯的关键。

- **关键桥接过程**：`BSQM_SMP_LOTFORM_RSLT`系列过程
  - 业务角色：通过读取包含钢卷和热卷信息的表，并将结果写回`SCR_COIL_MASTER`，这些过程在数据流中扮演了重要的角色，实现了从钢卷到热卷的信息传递。

## 4. 断点与不确定点

- **断点**：
  - 反向追溯中，除了`SCR_CC_HC_LNK_MGT`表外，没有其他明确的桥接过程或表直接支持从热卷回溯到钢卷，这可能是一个潜在的断点。

- **不确定点**：
  - `待确认`：是否存在其他未提及的过程或表，能够支持从热卷到钢卷的追溯。
  - `待确认`：`SCR_CC_HC_LNK_MGT`表中`COIL_NO`和`HCOIL_NO`字段的具体业务逻辑和数据更新机制。
```

#### 8. 钢板(成品板)（`PLT_NO`）→ 试样（`SMP_NO`）

```markdown
## 钢板(成品板) 到 试样 的追溯链路理解

### 1. **正向追溯**
由上游 **钢板(成品板)** 如何定位其下游 **试样**：

- **桥接表**：在提供的桥接表中，`SPR_SSM_BK` 同时含有 `PLT_NO` 和 `SMP_NO`，表明此表可能作为钢板到试样的直接桥接表。通过查询 `SPR_SSM_BK` 表中的 `PLT_NO` 和 `SMP_NO` 字段，可以建立钢板和试样之间的直接联系。

- **桥接过程**：在提供的桥接过程中，没有直接提到写入 `SMP_NO` 的过程，因此无法确定具体的存储过程如何将钢板信息关联到试样。这需要进一步确认是否存在其他未列出的过程或动态SQL，能够实现这一关联。

### 2. **反向追溯**
由 **试样** 如何回溯到 **钢板(成品板)**：

- **桥接表**：同样，`SPR_SSM_BK` 表提供了从试样回溯到钢板的直接途径。通过查询 `SMP_NO` 并关联 `PLT_NO`，可以实现从试样到钢板的追溯。

- **桥接过程**：与正向追溯类似，没有直接的过程提及从试样写入钢板信息的步骤，因此反向追溯的具体实现过程也需要进一步确认。

### 3. **关键桥接点**
最能承载此血缘的表/过程：

- **表**：`SPR_SSM_BK` 表是关键的桥接点，因为它同时包含 `PLT_NO` 和 `SMP_NO`，能够直接关联钢板和试样。业务角色上，此表可能用于记录钢板到试样的转换或采样信息，是追溯过程中的核心数据载体。

- **过程**：由于没有直接的过程提及钢板到试样或试样到钢板的写入操作，无法确定具体的关键过程。需要进一步分析系统日志或代码，以确定实现这一追溯的关键存储过程。

### 4. **断点与不确定点**
链路可能断裂或证据不足处，逐条标「待确认」：

- **桥接过程**：由于提供的桥接过程中没有直接涉及 `SMP_NO` 的写入操作，无法确定钢板到试样或试样到钢板的具体追溯过程。这需要进一步的系统分析或文档支持来「待确认」。

- **动态SQL和未覆盖的过程**：可能存在未列出的过程或动态SQL语句，这些可能影响追溯链路的完整性。这些部分需要通过系统日志分析或代码审查来「待确认」。

- **数据完整性和更新逻辑**：`SPR_SSM_BK` 表的数据完整性和更新逻辑也是关键因素，需要确认数据的准确性和更新机制，以确保追溯链路的可靠性。这一点同样需要「待确认」。
```

#### 9. 板坯（`SLAB_NO`）→ 试样（`SMP_NO`）

### 追溯边的链路理解

#### 1. **正向追溯**：由上游 板坯 如何定位其下游 试样

- **桥接表**：
  - `SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 表中同时包含 `SLAB_NO` 和 `SMP_NO`，表明板坯和试样之间存在直接关联。
  - `SCH_HSM_WORK_DETAIL_INFO` 表也包含这两个键，可能记录了板坯到试样的详细加工信息。

- **桥接过程**：
  - `BSCM_PLAN_RSLT_CONF` 过程读取 `SCH_HSM_ROLL_DESIGN_SLAB` 并写入 `SCH_HSM_ROLL_DESIGN_SLAB`、`SCH_HSM_ROLL_DESIGN_SLABT` 和 `SCH_HSM_WORK_DETAIL_INFO`，表明该过程可能在板坯设计结果确认时，将板坯信息与试样信息关联起来。

#### 2. **反向追溯**：由试样 如何回溯到 板坯

- **桥接表**：
  - 同正向追溯，`SCH_HSM_ROLL_DESIGN_SLAB`、`SCH_HSM_ROLL_DESIGN_SLABT` 和 `SCH_HSM_WORK_DETAIL_INFO` 表提供了从试样回溯到板坯的直接路径。

- **桥接过程**：
  - `BSCM_PLAN_RSLT_CONF` 过程同样适用于反向追溯，因为它在处理板坯设计结果时，涉及试样信息的更新。

#### 3. **关键桥接点**

- **表**：
  - `SCH_HSM_ROLL_DESIGN_SLAB` 和 `SCH_HSM_ROLL_DESIGN_SLABT` 是关键桥接表，因为它们作为主表包含了板坯的详细信息，并且与试样信息直接关联。
  - **业务角色**：这些表可能用于记录板坯的设计和加工信息，以及与试样的关联信息。

- **过程**：
  - `BSCM_PLAN_RSLT_CONF` 是关键桥接过程，因为它在板坯设计结果确认时，处理板坯和试样信息的更新。
  - **业务角色**：该过程可能用于确认板坯的设计结果，并确保试样信息与板坯信息的一致性和准确性。

#### 4. **断点与不确定点**

- **桥接表**：
  - 其他包含 `SLAB_NO` 和 `SMP_NO` 的表（如 `SCH_COLD_WORK_DESIGN_COIL`、`SCH_HOT_REVISE_PLAN_COIL` 等）的具体业务逻辑和数据流尚不清楚，需要进一步确认它们在板坯到试样追溯链中的作用。
  - **待确认**：这些表是否在板坯和试样的追溯链中扮演关键角色。

- **桥接过程**：
  - 除了 `BSCM_PLAN_RSLT_CONF` 外，其他涉及 `SLAB_NO` 和 `SMP_NO` 的过程（如 `BSCM_PROD_INST`、`BSCM_WORK_INST_PLT` 等）的具体业务逻辑和数据流也需要进一步确认。
  - **待确认**：这些过程是否在板坯和试样的追溯链中扮演关键角色，以及它们如何处理板坯和试样之间的数据关联。

以上分析基于提供的桥接表和桥接过程信息，具体的业务逻辑和数据流可能需要进一步的业务知识和系统文档来确认。

#### 10. 试样（`SMP_NO`）→ 质保/材质（`MTC_NO`）

以下是对追溯边“试样（键 SMP_NO） → 质保/材质（键 MTC_NO）”的链路理解：

### 1. 正向追溯：由上游试样如何定位其下游质保/材质

**桥接表**：
- **SQM_MTC_MECH**：同时包含试样键（SMP_NO）和质保/材质键（MTC_NO），是最直接的父子血缘证据。

**桥接过程**：
- 无直接存储过程将试样（SMP_NO）和质保/材质（MTC_NO）直接关联。

**结论**：
正向追溯主要依赖于桥接表**SQM_MTC_MECH**。通过查询该表，可以直接定位试样与其对应的质保/材质信息。

### 2. 反向追溯：由质保/材质如何回溯到试样

**桥接表**：
- **SQM_MTC_MECH**：同样适用于反向追溯，通过查询该表，可以从质保/材质回溯到对应的试样。

**桥接过程**：
- 无直接存储过程将质保/材质（MTC_NO）回溯到试样（SMP_NO）。

**结论**：
反向追溯同样依赖于桥接表**SQM_MTC_MECH**。通过查询该表，可以直接从质保/材质回溯到对应的试样信息。

### 3. 关键桥接点

**桥接表**：
- **SQM_MTC_MECH**：作为同时包含试样键（SMP_NO）和质保/材质键（MTC_NO）的表，是承载此血缘关系的关键桥接点。它直接关联试样和质保/材质，是实现追溯的关键数据载体。

**业务角色**：
- **SQM_MTC_MECH**表在业务流程中扮演着将试样的测试结果与对应的质保/材质信息关联起来的角色，为追溯提供直接的数据支持。

### 4. 断点与不确定点

- **桥接过程**：当前提供的存储过程中，没有直接将试样（SMP_NO）和质保/材质（MTC_NO）进行关联的过程。这可能是一个待确认的点，需要进一步分析是否有其他未列出的过程或动态SQL实现了这一关联。

综上所述，试样与质保/材质之间的追溯链路主要依赖于桥接表**SQM_MTC_MECH**。该表直接关联了试样和质保/材质，是实现正向和反向追溯的关键。然而，存储过程中缺少直接的关联操作，这可能是一个需要进一步确认的点。

#### 11. 钢板(成品板)（`PLT_NO`）→ 质保/材质（`MTC_NO`）

```markdown
## 追溯边链路理解：钢板(成品板)（键 PLT_NO）→ 质保/材质（键 MTC_NO）

### 1. 正向追溯：由上游 钢板(成品板) 如何定位其下游 质保/材质

由于没有直接的桥接表同时包含 PLT_NO 和 MTC_NO，正向追溯需要依赖存储过程来实现。在给定的桥接过程中，`BSQM_TOT_JDG_RSLT_CB` 存储过程从 `SQM_MTC_PROD` 表读取数据并写入 `SPR_DEFECT` 和 `SPR_PLATE` 表。这意味着，虽然 `SQM_MTC_PROD` 表包含 MTC_NO，但没有直接的证据表明它与 PLT_NO 直接关联。因此，正向追溯需要通过 `BSQM_TOT_JDG_RSLT_CB` 过程来实现，但具体的追溯路径和数据流需要进一步确认。

### 2. 反向追溯：由 质保/材质 如何回溯到 钢板(成品板)

反向追溯同样面临没有直接桥接表的问题。`SQM_MTC_PROD` 表包含 MTC_NO，但缺乏直接与 PLT_NO 相关联的证据。因此，反向追溯也需要依赖于存储过程，特别是 `BSQM_TOT_JDG_RSLT_CB` 过程，它从 `SQM_MTC_PROD` 读取数据。然而，由于缺乏直接的桥接表，这一追溯路径的具体实现和有效性需要进一步确认。

### 3. 关键桥接点

- **存储过程 `BSQM_TOT_JDG_RSLT_CB`**：作为关键的桥接点，该过程连接了 `SQM_MTC_PROD` 表（包含 MTC_NO）和 `SPR_DEFECT`、`SPR_PLATE` 表。尽管没有直接的桥接表，该过程在数据流中扮演了重要的角色，通过它可以实现从质保/材质到钢板(成品板)的追溯。业务角色上，`BSQM_TOT_JDG_RSLT_CB` 过程可能负责将材质信息与产品质量缺陷或成品板属性相关联，这对于质量控制和产品追踪至关重要。

### 4. 断点与不确定点

- **直接桥接表的缺失**：由于没有直接的桥接表同时包含 PLT_NO 和 MTC_NO，追溯链路的完整性和准确性依赖于存储过程的实现细节，这增加了追溯的复杂性和不确定性。**待确认**
- **存储过程 `BSQM_TOT_JDG_RSLT_CB` 的具体实现**：该过程如何实现从 `SQM_MTC_PROD` 到 `SPR_DEFECT` 和 `SPR_PLATE` 的数据流，以及这些表如何与 PLT_NO 和 MTC_NO 相关联，需要进一步的代码审查和业务逻辑分析来确认。**待确认**
```

#### 12. 钢板(成品板)（`PLT_NO`）→ 捆包（`BUND_NO`）

```markdown
## 追溯边链路理解：钢板(成品板)（键 PLT_NO） → 捆包（键 BUND_NO）

### 1. **正向追溯**
由上游钢板(成品板)定位其下游捆包，主要依据以下桥接表和过程：

- **桥接表**：
  - `SPR_BUND_RSLT`：同时包含PLT_NO和BUND_NO，作为父子血缘的直接证据。
  - `SSM_BUND_RSLT`：同样包含PLT_NO和BUND_NO，提供另一条正向追溯路径。

- **桥接过程**：
  - `BSPR_BUND_OBOUT`：读取`SPR_BUND_RSLT`和`SPR_PLATE`，写入`SPR_MAX_BUND`和`SPR_PLATE`，表明该过程在钢板和捆包之间建立了联系。

### 2. **反向追溯**
由捆包回溯到钢板(成品板)，主要依据以下桥接表和过程：

- **桥接表**：
  - `SPR_BUND_RSLT`：包含BUND_NO和PLT_NO，为反向追溯提供直接证据。
  - `SSM_BUND_RSLT`：同样包含BUND_NO和PLT_NO，提供另一条反向追溯路径。

- **桥接过程**：
  - `BSPR_CUTTING_SEARCH`：读取`SPR_CUTTING_RSLT`、`SPR_CUT_CS_DTL_RSLT`、`SPR_CUT_CS_RSLT`，写入`SPR_PLATE`，表明该过程可能涉及从捆包追溯到钢板的逻辑。

### 3. **关键桥接点**
- **表**：
  - `SPR_BUND_RSLT`：作为最关键的桥接表，因为它同时包含PLT_NO和BUND_NO，直接体现了钢板和捆包之间的血缘关系。在业务流程中，这张表可能用于记录钢板到捆包的转换结果，是追溯链路的核心。

- **过程**：
  - `BSPR_BUND_OBOUT`：作为最关键的桥接过程，因为它直接读取钢板和捆包的相关信息，并更新捆包和钢板的状态，体现了从钢板到捆包的转换过程。

### 4. **断点与不确定点**
- `BSPR_CUTTING_SEARCH`过程虽然涉及从切割结果表写入钢板表，但具体是否涉及从捆包回溯到钢板的逻辑，**待确认**。
- 其他未列出的过程可能也涉及钢板和捆包之间的追溯，但具体细节**待确认**。
- 动态SQL或未覆盖的过程可能影响追溯链路的完整性，这些部分的具体情况**待确认**。
```

#### 13. 订单（`ORD_NO`）→ 质保/材质（`MTC_NO`）

以下是对订单（键 ORD_NO）到质保/材质（键 MTC_NO）追溯链路的理解：

### 1. 正向追溯：由上游订单定位其下游质保/材质

- **桥接表**：订单与质保/材质之间存在多个桥接表，包括 `SQM_MTC_COM`、`SQM_MTC_PROD`、`SQM_MTC_REQ` 等，这些表同时包含 `ORD_NO` 和 `MTC_NO`，表明它们可以作为订单与质保/材质之间的直接关联。
- **桥接过程**：在提供的存储过程中，`BSQM_MTC_ISSUE`、`BSQM_MTC_ISSUE_HB`、`BSQM_MTC_ISSUE_LZ`、`BSQM_MTC_ISSUE_RZ`、`BSQM_MTC_ISSUE_SHIP`、`BSQM_MTC_ISSUE_SHIP_YC` 和 `BSQM_MTC_ISSUE_SHIP_YC1` 都涉及到从订单相关的表（如 `SQM_ORD_CHEM`）读取数据，并写入到质保/材质相关的表（如 `SQM_MTC_REQ`、`SQM_WSP_MTC_CHEM_INF`、`SQM_WSP_MTC_COM_INF` 等）。这些过程表明订单信息被用来生成或更新质保/材质信息。

### 2. 反向追溯：由质保/材质回溯到订单

- **桥接表**：同样，通过 `SQM_MTC_COM`、`SQM_MTC_PROD`、`SQM_MTC_REQ` 等桥接表，可以从质保/材质信息追溯到订单信息，因为这些表包含 `ORD_NO`。
- **桥接过程**：在反向追溯中，`BSQM_MTC_ISSUE`、`BSQM_MTC_ISSUE_HB` 等过程从质保/材质相关的表读取数据，并写入到订单相关的表，这表明可以通过这些过程回溯到订单信息。

### 3. 关键桥接点

- **表**：`SQM_MTC_REQ` 是一个关键的桥接表，因为它在多个过程中被用作读写对象，表明它在订单与质保/材质之间的信息传递中扮演重要角色。
- **过程**：`BSQM_MTC_ISSUE` 系列过程是关键的桥接过程，因为它们不仅涉及从订单到质保/材质的信息流，也涉及从质保/材质到订单的信息流，这些过程在业务流程中起到了核心的连接作用。

### 4. 断点与不确定点

- **待确认**：由于没有提供所有存储过程的详细代码，无法完全确定所有过程是否都涉及到 `ORD_NO` 和 `MTC_NO` 的直接关联。特别是那些没有在 P0 存储过程中被提及的过程，如 `BSQM_TOT_JDG_RSLT_CB`，其是否涉及到订单与质保/材质的追溯链路需要进一步确认。
- **待确认**：动态 SQL 的使用可能会影响追溯链路的确定性，因为它们可能在运行时动态地引用不同的表或字段，这部分的追溯链路也需要进一步确认。

综上所述，订单与质保/材质之间的追溯链路主要通过 `SQM_MTC_REQ` 等桥接表和 `BSQM_MTC_ISSUE` 系列桥接过程来实现，但部分过程和动态 SQL 的使用可能引入不确定性。


---

### 全链路总览

### MES 端到端追溯链路总览理解

#### 端到端主链路
1. **订单 → 计划炉次**：通过桥表28和桥过程20连接，订单信息通过计划转化为具体的炉次。
2. **计划炉次 → 炉次**：通过桥表36和桥过程22连接，将计划炉次具体化为实际的炉次。
3. **炉次 → 板坯**：通过桥表47和桥过程26连接，炉次对应的板坯信息被记录。
4. **板坯 → 板/卷**：分为两个分支，板坯到钢板通过桥表34和桥过程19连接，板坯到钢卷通过桥表10和桥过程16连接。
5. **板/卷 → 试样**：钢板到试样通过桥表8和桥过程17连接，板坯到试样通过桥表40和桥过程25连接。
6. **试样 → 质保/材质**：通过桥表12和桥过程8连接，试样信息最终关联到质保和材质信息。

#### 关键分叉
- **板 vs 卷**：板坯作为分叉点，一方面可以转化为钢板（成品板），另一方面可以转化为钢卷。这两个产品路线分别通过不同的桥接表和过程进行追溯。

#### 全链路的追溯断点风险与「待确认」清单
- **追溯断点风险**：在订单到质保/材质的追溯链路中，存在一个断点，即从钢板（成品板）到质保/材质的桥接表为0，桥过程为1，这可能意味着这部分追溯链路存在数据缺失或不完整的风险。
- **待确认清单**：
  - 钢板（成品板）到质保/材质的过程和表是否完整，需要进一步确认。
  - 由于动态SQL和部分过程未被覆盖（P0未覆盖的过程），可能存在未被识别的追溯链路或断点，这些都需要进一步的确认和分析。

综上所述，MES系统通过一系列的桥接表和桥接过程，实现了从订单到质保/材质的端到端追溯。然而，全链路中存在潜在的追溯断点和风险，需要进一步的确认和优化以确保数据的完整性和追溯的准确性。

