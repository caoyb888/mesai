## 模块：审计日志

**系统**：ITSM | **接口数**：2 | **模块说明**：操作审计、登录日志查询

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/audit-logs` | 查询审计日志 | ✅ JWT |
| **GET** | `/audit-logs/login` | 查询登录日志 | ✅ JWT |

### 接口详情

#### GET /audit-logs

**功能**：查询审计日志
**operationId**：`listAuditLogs`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `userId` | string |  |  |
| `accountNo` | string |  |  |
| `module` | string |  |  |
| `action` | string |  |  |
| `resourceType` | string |  |  |
| `resourceId` | string |  |  |
| `result` | string[SUCCESS/FAIL] |  |  |
| `loginIp` | string |  |  |
| `startTime` | string |  |  |
| `endTime` | string |  |  |
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 审计日志列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object |  |  |

#### GET /audit-logs/login

**功能**：查询登录日志
**operationId**：`listLoginLogs`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `accountNo` | string |  |  |
| `loginIp` | string |  |  |
| `result` | string[SUCCESS/FAIL] |  |  |
| `startTime` | string |  |  |
| `endTime` | string |  |  |
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 登录日志列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

---
*chunk_id: itsm_审计日志_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*