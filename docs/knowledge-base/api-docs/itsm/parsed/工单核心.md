## 模块：工单核心

**系统**：ITSM | **接口数**：14 | **模块说明**：工单生命周期管理（创建、接单、转单、挂起、关闭等）

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/tickets` | 查询工单列表 | ✅ JWT |
| **POST** | `/tickets` | 创建工单 | ✅ JWT |
| **GET** | `/tickets/{id}` | 获取工单详情 | ✅ JWT |
| **POST** | `/tickets/{id}/claim` | 接单 | ✅ JWT |
| **POST** | `/tickets/{id}/transfer` | 转单 | ✅ JWT |
| **POST** | `/tickets/{id}/suspend` | 挂起 | ✅ JWT |
| **POST** | `/tickets/{id}/resume` | 恢复 | ✅ JWT |
| **POST** | `/tickets/{id}/submit-review` | 提交审核 | ✅ JWT |
| **POST** | `/tickets/{id}/approve` | 审核通过 | ✅ JWT |
| **POST** | `/tickets/{id}/reject` | 审核驳回 | ✅ JWT |
| **POST** | `/tickets/{id}/close` | 关闭工单 | ✅ JWT |
| **GET** | `/tickets/{id}/flow-logs` | 工单流转历史 | ✅ JWT |
| **GET** | `/tickets/{id}/comments` | 获取评论列表 | ✅ JWT |
| **POST** | `/tickets/{id}/comments` | 添加评论 | ✅ JWT |

### 接口详情

#### GET /tickets

**功能**：查询工单列表
**说明**：数据范围由角色决定
**operationId**：`listTickets`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |
| `keyword` | string |  | 搜索关键词 |
| `status` | string |  | 状态筛选，多个用逗号分隔 |
| `priority` | string[LOW/MEDIUM/HIGH/CRITICAL] |  |  |
| `modelId` | string |  |  |
| `assignedGroupId` | string |  |  |
| `assignedUserId` | string |  |  |
| `createdUserId` | string |  |  |
| `slaStatus` | string[NORMAL/WARNING/BREACH] |  | SLA 状态筛选 |
| `createdAtStart` | string |  |  |
| `createdAtEnd` | string |  |  |
| `orderBy` | string |  | 排序字段 |
| `orderDir` | string[asc/desc] |  | 排序方向 |

**响应（200）：** 工单列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object |  |  |

#### POST /tickets

**功能**：创建工单
**说明**：支持内部用户和外部系统（API Key）创建
**operationId**：`createTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `modelId` | string | ✅ | 服务模型 ID |
| `title` | string | ✅ |  |
| `description` | string |  |  |
| `priority` | string[LOW/MEDIUM/HIGH/CRITICAL] | ✅ |  |
| `formData` | object |  | 动态表单数据，键值对形式 |
| `ciIds` | array(string) |  | 关联配置项 ID 列表 |
| `source` | string |  | API Key 创建时可选，默认为 API |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(TicketBasicInfo) |  |  |
| `data.id` | string |  |  |
| `data.ticketNo` | string |  |  |
| `data.status` | string |  |  |
| `data.title` | string |  |  |
| `data.priority` | string |  |  |
| `data.assignedGroupId` | string |  |  |
| `data.assignedGroupName` | string |  |  |
| `data.slaDeadlineResponse` | string |  |  |
| `data.slaDeadlineResolve` | string |  |  |
| `data.createdAt` | string |  |  |

#### GET /tickets/{id}

**功能**：获取工单详情
**operationId**：`getTicketById`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 工单详情

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(TicketDetail) |  |  |

#### POST /tickets/{id}/claim

**功能**：接单
**说明**：当前用户自动成为处理人，需目标用户组成员权限
**operationId**：`claimTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| （见接口说明）| - | - | - |

**响应（200）：** 接单成功或业务错误（已被他人接单等）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/transfer

**功能**：转单
**说明**：当前处理人或管理员权限
**operationId**：`transferTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `targetGroupId` | string | ✅ | 目标用户组 ID |
| `targetUserId` | string |  | 目标处理人 ID（可选） |
| `reason` | string |  | 转单原因 |

**响应（200）：** 转单成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/suspend

**功能**：挂起
**说明**：仅当前处理人可操作
**operationId**：`suspendTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `reason` | string | ✅ | 挂起原因 |
| `expectedResumeAt` | string |  | 预计恢复时间 |

**响应（200）：** 挂起成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/resume

**功能**：恢复
**说明**：仅当前处理人可操作
**operationId**：`resumeTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `remark` | string |  |  |

**响应（200）：** 恢复成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/submit-review

**功能**：提交审核
**operationId**：`submitTicketReview`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `resolveDescription` | string | ✅ | 解决方案描述 |
| `knowledgeRefIds` | array(string) |  | 关联知识库条目 ID 列表 |

**响应（200）：** 提交成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/approve

**功能**：审核通过
**说明**：审核员或管理员权限
**operationId**：`approveTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 审核通过

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/reject

**功能**：审核驳回
**说明**：审核员或管理员权限
**operationId**：`rejectTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `reason` | string | ✅ | 驳回原因 |

**响应（200）：** 已驳回

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /tickets/{id}/close

**功能**：关闭工单
**说明**：Created 状态需管理员权限，Processing 状态需当前处理人权限
**operationId**：`closeTicket`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `reason` | string | ✅ | 关闭原因 |

**响应（200）：** 关闭成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /tickets/{id}/flow-logs

**功能**：工单流转历史
**operationId**：`getTicketFlowLogs`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 流转历史列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(TicketFlowLog) |  |  |

#### GET /tickets/{id}/comments

**功能**：获取评论列表
**说明**：isInternal=true 的评论仅运维人员可见
**operationId**：`listTicketComments`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 评论列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(TicketComment) |  |  |

#### POST /tickets/{id}/comments

**功能**：添加评论
**operationId**：`addTicketComment`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `content` | string | ✅ |  |
| `isInternal` | boolean |  | 是否内部评论（客户不可见） |
| `attachmentIds` | array(string) |  |  |

**响应（200）：** 添加成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

---
*chunk_id: itsm_工单核心_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*