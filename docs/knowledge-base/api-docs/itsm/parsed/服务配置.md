## 模块：服务配置

**系统**：ITSM | **接口数**：7 | **模块说明**：服务目录、服务模型、工作流定义

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/service-catalogs/tree` | 获取服务目录树 | ✅ JWT |
| **GET** | `/service-models` | 获取服务模型列表 | ✅ JWT |
| **POST** | `/service-models` | 创建服务模型 | ✅ JWT |
| **GET** | `/service-models/{id}/form-fields` | 获取服务模型表单字段 | ✅ JWT |
| **PUT** | `/service-models/{id}/form-fields` | 更新表单字段（批量） | ✅ JWT |
| **GET** | `/workflow-defs` | 获取工作流列表 | ✅ JWT |
| **GET** | `/workflow-defs/{id}` | 获取工作流详情（含状态机配置） | ✅ JWT |

### 接口详情

#### GET /service-catalogs/tree

**功能**：获取服务目录树
**operationId**：`getServiceCatalogTree`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 服务目录树

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(ServiceCatalogNode) |  |  |

#### GET /service-models

**功能**：获取服务模型列表
**operationId**：`listServiceModels`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `catalogId` | string |  |  |
| `status` | integer |  |  |
| `keyword` | string |  |  |
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 服务模型列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

#### POST /service-models

**功能**：创建服务模型
**operationId**：`createServiceModel`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `catalogId` | string | ✅ |  |
| `name` | string | ✅ |  |
| `code` | string | ✅ |  |
| `description` | string |  |  |
| `workflowDefId` | string |  |  |
| `slaPolicyId` | string |  |  |
| `defaultGroupId` | string |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /service-models/{id}/form-fields

**功能**：获取服务模型表单字段
**operationId**：`getServiceModelFormFields`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 表单字段列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(FormField) |  |  |

#### PUT /service-models/{id}/form-fields

**功能**：更新表单字段（批量）
**operationId**：`updateServiceModelFormFields`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `fields` | array(FormField) | ✅ |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /workflow-defs

**功能**：获取工作流列表
**operationId**：`listWorkflowDefs`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 工作流列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(WorkflowDef) |  |  |

#### GET /workflow-defs/{id}

**功能**：获取工作流详情（含状态机配置）
**operationId**：`getWorkflowDefById`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 工作流详情

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(WorkflowDefDetail) |  |  |

---
*chunk_id: itsm_服务配置_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*