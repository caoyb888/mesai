## 模块：消息推送渠道

**系统**：ITSM | **接口数**：5 | **模块说明**：通知渠道配置、测试、开关控制

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/notify/channels` | 获取渠道配置列表 | ✅ JWT |
| **PATCH** | `/notify/channels/{id}` | 更新渠道配置 | ✅ JWT |
| **POST** | `/notify/channels/{id}/enable` | 启用渠道 | ✅ JWT |
| **POST** | `/notify/channels/{id}/disable` | 禁用渠道（强管控开关） | ✅ JWT |
| **POST** | `/notify/channels/{id}/test` | 测试渠道连通性 | ✅ JWT |

### 接口详情

#### GET /notify/channels

**功能**：获取渠道配置列表
**operationId**：`listNotifyChannels`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 渠道列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(NotifyChannel) |  |  |

#### PATCH /notify/channels/{id}

**功能**：更新渠道配置
**operationId**：`updateNotifyChannel`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `configJson` | object |  | 渠道配置（敏感字段脱敏返回） |
| `rateLimitCount` | integer |  |  |
| `dailyQuota` | integer |  |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /notify/channels/{id}/enable

**功能**：启用渠道
**说明**：操作强制写入审计日志
**operationId**：`enableNotifyChannel`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 已启用

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /notify/channels/{id}/disable

**功能**：禁用渠道（强管控开关）
**说明**：操作强制写入审计日志，返回待发消息数量
**operationId**：`disableNotifyChannel`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 已禁用

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object |  |  |
| `data.channelType` | string |  |  |
| `data.previousStatus` | integer |  |  |
| `data.currentStatus` | integer |  |  |
| `data.pendingMessageCount` | integer |  |  |
| `data.message` | string |  |  |

#### POST /notify/channels/{id}/test

**功能**：测试渠道连通性
**operationId**：`testNotifyChannel`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `recipient` | string | ✅ | 测试接收地址（邮箱/手机号等） |

**响应（200）：** 测试结果

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object |  |  |
| `data.success` | boolean |  |  |
| `data.latencyMs` | integer |  |  |
| `data.message` | string |  |  |

---
*chunk_id: itsm_消息推送渠道_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*