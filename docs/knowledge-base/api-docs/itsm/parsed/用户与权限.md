## 模块：用户与权限

**系统**：ITSM | **接口数**：13 | **模块说明**：用户管理、用户组、角色与权限配置

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/users` | 获取用户列表 | ✅ JWT |
| **POST** | `/users` | 创建用户（管理员直接创建） | ✅ JWT |
| **GET** | `/users/{id}` | 获取用户详情 | ✅ JWT |
| **PATCH** | `/users/{id}` | 更新用户信息 | ✅ JWT |
| **POST** | `/users/{id}/approve` | 审核通过用户注册申请 | ✅ JWT |
| **POST** | `/users/{id}/reject` | 驳回用户注册申请 | ✅ JWT |
| **PUT** | `/users/{id}/roles` | 分配用户角色 | ✅ JWT |
| **GET** | `/user-groups` | 获取用户组列表 | ✅ JWT |
| **POST** | `/user-groups` | 创建用户组 | ✅ JWT |
| **PUT** | `/user-groups/{id}/members` | 更新用户组成员 | ✅ JWT |
| **GET** | `/roles` | 获取角色列表 | ✅ JWT |
| **GET** | `/permissions/tree` | 获取权限树 | ✅ JWT |
| **PUT** | `/roles/{id}/permissions` | 更新角色权限 | ✅ JWT |

### 接口详情

#### GET /users

**功能**：获取用户列表
**说明**：支持分页、搜索、部门筛选、状态筛选
**operationId**：`listUsers`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |
| `keyword` | string |  | 搜索关键词 |
| `deptId` | string |  | 部门 ID 筛选 |
| `status` | integer[0/1/2] |  | 状态筛选（0禁用 1启用 2待审核） |

**响应（200）：** 用户列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

#### POST /users

**功能**：创建用户（管理员直接创建）
**说明**：初始密码由系统生成后通过邮件发送
**operationId**：`createUser`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `accountNo` | string | ✅ |  |
| `employeeName` | string | ✅ |  |
| `email` | string | ✅ |  |
| `phone` | string | ✅ |  |
| `deptId` | string | ✅ |  |
| `roleIds` | array(string) |  |  |
| `groupIds` | array(string) |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(UserBasicInfo) |  |  |
| `data.id` | string |  | 用户 ID（字符串形式） |
| `data.accountNo` | string |  |  |
| `data.employeeName` | string |  |  |
| `data.email` | string |  |  |
| `data.phone` | string |  |  |
| `data.roles` | array(string) |  |  |
| `data.deptId` | string |  |  |
| `data.deptName` | string |  |  |

#### GET /users/{id}

**功能**：获取用户详情
**operationId**：`getUserById`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 用户详情

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(UserDetail) |  |  |

#### PATCH /users/{id}

**功能**：更新用户信息
**说明**：所有字段可选，仅传需更新的字段。本人仅可修改邮箱、手机号、头像
**operationId**：`updateUser`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `employeeName` | string |  |  |
| `email` | string |  |  |
| `phone` | string |  |  |
| `deptId` | string |  |  |
| `status` | integer[0/1/2] |  |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /users/{id}/approve

**功能**：审核通过用户注册申请
**operationId**：`approveUser`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 审核通过

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /users/{id}/reject

**功能**：驳回用户注册申请
**operationId**：`rejectUser`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `reason` | string | ✅ | 驳回原因 |

**响应（200）：** 已驳回

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### PUT /users/{id}/roles

**功能**：分配用户角色
**operationId**：`assignUserRoles`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `roleIds` | array(string) | ✅ | 角色 ID 列表 |

**响应（200）：** 分配成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /user-groups

**功能**：获取用户组列表
**operationId**：`listUserGroups`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 用户组列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

#### POST /user-groups

**功能**：创建用户组
**operationId**：`createUserGroup`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | 组名称 |
| `deptId` | string | ✅ | 部门 ID |
| `description` | string |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### PUT /user-groups/{id}/members

**功能**：更新用户组成员
**operationId**：`updateGroupMembers`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `members` | array(object) | ✅ |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /roles

**功能**：获取角色列表
**operationId**：`listRoles`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 角色列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(Role) |  |  |

#### GET /permissions/tree

**功能**：获取权限树
**operationId**：`getPermissionTree`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 权限树结构

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(PermissionNode) |  |  |

#### PUT /roles/{id}/permissions

**功能**：更新角色权限
**operationId**：`updateRolePermissions`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `permissionIds` | array(string) | ✅ |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

---
*chunk_id: itsm_用户与权限_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*