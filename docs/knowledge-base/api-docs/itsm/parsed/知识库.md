## 模块：知识库

**系统**：ITSM | **接口数**：10 | **模块说明**：知识条目、分类、审核流程

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/kb/articles` | 获取知识条目列表 | ✅ JWT |
| **POST** | `/kb/articles` | 创建知识条目（草稿） | ✅ JWT |
| **POST** | `/kb/articles/{id}/submit-review` | 提交审核 | ✅ JWT |
| **POST** | `/kb/articles/{id}/approve` | 审核通过 | ✅ JWT |
| **POST** | `/kb/articles/{id}/reject` | 审核驳回 | ✅ JWT |
| **GET** | `/kb/articles/{id}/versions` | 获取版本历史 | ✅ JWT |
| **GET** | `/kb/categories/tree` | 获取分类树 | ✅ JWT |
| **POST** | `/kb/categories` | 创建分类 | ✅ JWT |
| **PATCH** | `/kb/categories/{id}` | 更新分类 | ✅ JWT |
| **DELETE** | `/kb/categories/{id}` | 删除分类 | ✅ JWT |

### 接口详情

#### GET /kb/articles

**功能**：获取知识条目列表
**说明**：Draft/Rejected 状态仅作者和审核员可见
**operationId**：`listKbArticles`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |
| `keyword` | string |  | 标题/关键词全文搜索 |
| `categoryId` | string |  |  |
| `status` | string[DRAFT/PENDING_REVIEW/APPROVED/REJECTED] |  |  |

**响应（200）：** 知识条目列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

#### POST /kb/articles

**功能**：创建知识条目（草稿）
**operationId**：`createKbArticle`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `categoryId` | string | ✅ |  |
| `title` | string | ✅ |  |
| `summary` | string |  |  |
| `keywords` | string |  |  |
| `content` | string | ✅ | HTML 富文本内容 |
| `probeRefId` | string |  |  |
| `attachmentIds` | array(string) |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /kb/articles/{id}/submit-review

**功能**：提交审核
**说明**：仅作者本人可操作
**operationId**：`submitKbArticleReview`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 提交成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /kb/articles/{id}/approve

**功能**：审核通过
**说明**：知识库审核员权限，且不能是作者本人
**operationId**：`approveKbArticle`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 审核通过

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### POST /kb/articles/{id}/reject

**功能**：审核驳回
**说明**：知识库审核员权限，且不能是作者本人
**operationId**：`rejectKbArticle`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `comment` | string | ✅ | 驳回意见 |

**响应（200）：** 已驳回

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /kb/articles/{id}/versions

**功能**：获取版本历史
**operationId**：`getKbArticleVersions`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 版本历史列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(KbArticleVersion) |  |  |

#### GET /kb/categories/tree

**功能**：获取分类树
**operationId**：`getKbCategoryTree`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**响应（200）：** 分类树

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(KbCategory) |  |  |

#### POST /kb/categories

**功能**：创建分类
**operationId**：`createKbCategory`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string | ✅ |  |
| `parentId` | string |  |  |
| `sortOrder` | integer |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### PATCH /kb/categories/{id}

**功能**：更新分类
**operationId**：`updateKbCategory`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | string |  |  |
| `sortOrder` | integer |  |  |

**响应（200）：** 更新成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### DELETE /kb/categories/{id}

**功能**：删除分类
**说明**：需无关联条目
**operationId**：`deleteKbCategory`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 删除成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

---
*chunk_id: itsm_知识库_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*