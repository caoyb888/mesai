## 模块：运维日历与排班

**系统**：ITSM | **接口数**：5 | **模块说明**：日历事件、值班排班、当日值班查询

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **GET** | `/calendar/events` | 获取日历事件列表 | ✅ JWT |
| **POST** | `/calendar/events` | 创建日历事件 | ✅ JWT |
| **GET** | `/roster/schedules` | 获取排班列表 | ✅ JWT |
| **POST** | `/roster/schedules/generate` | 批量生成排班 | ✅ JWT |
| **GET** | `/roster/on-duty` | 查询当日值班人 | ✅ JWT |

### 接口详情

#### GET /calendar/events

**功能**：获取日历事件列表
**operationId**：`listCalendarEvents`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `startTime` | string | ✅ |  |
| `endTime` | string | ✅ |  |
| `eventType` | string |  |  |
| `groupId` | string |  |  |

**响应（200）：** 事件列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | array(CalendarEvent) |  |  |

#### POST /calendar/events

**功能**：创建日历事件
**operationId**：`createCalendarEvent`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | ✅ |  |
| `eventType` | string | ✅ |  |
| `description` | string |  |  |
| `startTime` | string | ✅ |  |
| `endTime` | string | ✅ |  |
| `allDay` | boolean |  |  |
| `relatedGroupId` | string |  |  |
| `color` | string |  |  |

**响应（200）：** 创建成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /roster/schedules

**功能**：获取排班列表
**operationId**：`listRosterSchedules`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `teamId` | string |  |  |
| `startDate` | string |  |  |
| `endDate` | string |  |  |
| `page` | integer |  | 页码，从 1 开始 |
| `pageSize` | integer |  | 每页条数，最大 100 |

**响应（200）：** 排班列表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(PageResponse) |  |  |
| `data.list` | array(object) |  |  |
| `data.total` | integer |  | 总记录数 |
| `data.page` | integer |  | 当前页码 |
| `data.pageSize` | integer |  | 每页条数 |
| `data.totalPages` | integer |  | 总页数 |

#### POST /roster/schedules/generate

**功能**：批量生成排班
**operationId**：`generateRosterSchedules`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `teamId` | string | ✅ |  |
| `startDate` | string | ✅ |  |
| `endDate` | string | ✅ |  |
| `shiftType` | string[DAY/NIGHT] | ✅ |  |
| `startTime` | string |  |  |
| `endTime` | string |  |  |
| `rotationDays` | integer |  | 轮班天数 |

**响应（200）：** 生成成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

#### GET /roster/on-duty

**功能**：查询当日值班人
**operationId**：`getOnDutyUser`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `date` | string |  | 日期，默认为今天 |
| `groupId` | string | ✅ | 用户组 ID |

**响应（200）：** 当日值班信息

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(OnDutyInfo) |  |  |
| `data.date` | string |  |  |
| `data.groupId` | string |  |  |
| `data.onDutyUser` | object |  |  |

---
*chunk_id: itsm_运维日历与排班_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*