## 模块：附件管理

**系统**：ITSM | **接口数**：3 | **模块说明**：文件上传、下载、删除

### 接口速查表

| 方法 | 路径 | 说明 | 是否需要鉴权 |
|------|------|------|------------|
| **POST** | `/attachments` | 上传附件 | ✅ JWT |
| **GET** | `/attachments/{id}/download` | 下载附件 | ✅ JWT |
| **DELETE** | `/attachments/{id}` | 删除附件 | ✅ JWT |

### 接口详情

#### POST /attachments

**功能**：上传附件
**说明**：支持 multipart/form-data 上传，最大 20MB
**operationId**：`uploadAttachment`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**请求体（application/json）：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| （见接口说明）| - | - | - |

**响应（200）：** 上传成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |
| `data` | object(Attachment) |  |  |
| `data.id` | string |  |  |
| `data.fileName` | string |  |  |
| `data.fileSize` | integer |  |  |
| `data.fileType` | string |  |  |
| `data.fileExt` | string |  |  |
| `data.downloadUrl` | string |  |  |
| `data.createdAt` | string |  |  |

#### GET /attachments/{id}/download

**功能**：下载附件
**说明**：直接返回文件流，校验与关联业务的数据权限
**operationId**：`downloadAttachment`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 文件流


#### DELETE /attachments/{id}

**功能**：删除附件
**说明**：逻辑删除，实体文件由定时任务清理。仅上传人本人或管理员可操作
**operationId**：`deleteAttachment`
**鉴权**：✅ 需要 Bearer Token（全局默认）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | string | ✅ |  |

**响应（200）：** 删除成功

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `code` | integer | ✅ | 业务状态码，6 位数字 |
| `message` | string | ✅ | 提示信息（中文） |
| `data` | object | ✅ | 业务数据，失败时为 null |
| `traceId` | string | ✅ | 链路追踪 ID |

---
*chunk_id: itsm_附件管理_api_module*
*chunk_type: api_module*
*关联任务：S2-2 T2-2-1 · 作者：AI（芯智云匠）· 日期：2026-04-13*